---
phase: 27-e2e-tests
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - frontend/e2e/free-scan.spec.ts
  - frontend/e2e/paid-audit.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-05]

must_haves:
  truths:
    - "Free scan test navigates from home page through URL/email submission to scan progress page"
    - "Free scan test verifies scan progress polling shows scanning UI, then redirects to results page"
    - "Results page shows grade, severity badges, finding details, and UpgradeCTA for free tier"
    - "CFAA consent test verifies form rejects submission without authorization checkbox"
    - "Paid audit test intercepts UpgradeCTA checkout redirect and verifies Stripe URL pattern"
    - "Payment success page renders after simulated Stripe redirect"
    - "Payment cancel/failure return URL is handled gracefully"
  artifacts:
    - path: "frontend/e2e/free-scan.spec.ts"
      provides: "Free scan flow E2E test from home to results"
      contains: "test.*scan"
    - path: "frontend/e2e/paid-audit.spec.ts"
      provides: "Paid audit flow E2E test with Stripe checkout"
      contains: "test.*checkout"
  key_links:
    - from: "frontend/e2e/free-scan.spec.ts"
      to: "frontend/e2e/helpers/fetch-mocks.ts"
      via: "import mockScanSubmission, mockResultsPage, mockScanCount"
      pattern: "import.*fetch-mocks"
    - from: "frontend/e2e/free-scan.spec.ts"
      to: "frontend/e2e/helpers/route-mocks.ts"
      via: "import mockScanPolling"
      pattern: "import.*route-mocks"
    - from: "frontend/e2e/paid-audit.spec.ts"
      to: "frontend/e2e/helpers/route-mocks.ts"
      via: "import mockCheckout"
      pattern: "import.*route-mocks"
---

<objective>
Write E2E tests for the two happy-path user journeys: the free scan flow (home page through results) and the paid audit flow (UpgradeCTA through Stripe checkout and return).

Purpose: Verify the critical revenue-generating user flows work end-to-end in a production-like browser environment with realistic API timing.
Output: Two spec files covering E2E-01 (free scan), E2E-02 (paid audit), and E2E-05 (Stripe test mode).
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-e2e-tests/27-CONTEXT.md
@.planning/phases/27-e2e-tests/27-RESEARCH.md
@.planning/phases/27-e2e-tests/27-01-SUMMARY.md
@frontend/app/page.tsx
@frontend/app/scan/[id]/page.tsx
@frontend/app/results/[token]/page.tsx
@frontend/app/payment/success/page.tsx
@frontend/components/scan-form.tsx
@frontend/components/upgrade-cta.tsx
@frontend/lib/types.ts
@frontend/e2e/fixtures/scan.ts
@frontend/e2e/fixtures/results.ts
@frontend/e2e/fixtures/checkout.ts
@frontend/e2e/helpers/route-mocks.ts
@frontend/e2e/helpers/fetch-mocks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write free scan flow E2E test</name>
  <files>frontend/e2e/free-scan.spec.ts</files>
  <action>
Create `frontend/e2e/free-scan.spec.ts` importing `test, expect` from `next/experimental/testmode/playwright`.

Import fixtures from `./fixtures/scan` and `./fixtures/results`. Import helpers from `./helpers/fetch-mocks` and `./helpers/route-mocks`.

**Test 1: "complete free scan flow from home to results"**

This is the full user journey test. Setup:
- `mockScanCount(next, 42)` — home page needs scan count
- `mockScanSubmission(next, scanFixtures)` — Server Action POST to backend
- `mockScanPolling(page, scanFixtures)` — client-side polling on scan progress page
- `mockResultsPage(next, resultsFixtures.freeGradeB)` — server-side fetch on results page

Steps:
1. `await page.goto('/')` — navigate to home page
2. Verify home page loaded: `await expect(page.locator('h1')).toContainText('Security scanning')` (matches the actual h1 text)
3. Fill the form:
   - `await page.fill('input[name="url"]', 'https://example.com')`
   - `await page.fill('input[name="email"]', 'test@example.com')`
   - `await page.check('input[name="authorization"]')` — CFAA consent checkbox
4. Submit: `await page.click('button[type="submit"]')`
5. Wait for navigation to scan progress page: `await page.waitForURL('**/scan/**', { timeout: 15000 })`
6. Verify progress UI: `await expect(page.locator('text=Scanning')).toBeVisible()`
7. Wait for scan to complete and redirect to results: `await page.waitForURL('**/results/**', { timeout: 30000 })`
8. Verify results page content:
   - `await expect(page.locator('h1')).toContainText('Security Scan Results')`
   - `await expect(page.locator('text=B')).toBeVisible()` — grade display
   - Verify severity badges are visible: `await expect(page.locator('text=high').first()).toBeVisible()` — the freeGradeB fixture includes a high-severity finding, and the FindingAccordion renders severity as an uppercase badge (CSS `uppercase` class on a span). Also check for medium: `await expect(page.locator('text=medium').first()).toBeVisible()`
   - Verify at least one finding is visible (use finding title from fixture)
   - `await expect(page.locator('text=Upgrade to Deep Audit')).toBeVisible()` — UpgradeCTA for free tier

**Test 2: "CFAA consent required before submission"**

Per locked decision: test that submitting without consent fails, then check it and submit successfully.

Setup:
- `mockScanCount(next, 0)` — home page needs scan count
- `mockScanSubmission(next, scanFixtures)` — for the second (successful) submission

Steps:
1. `await page.goto('/')`
2. Fill URL and email but do NOT check authorization checkbox
3. Click submit
4. Verify validation error: `await expect(page.locator('text=You must confirm you have authorization')).toBeVisible()`
5. Now check the authorization checkbox
6. Also set up the scan polling mock (mockScanPolling) since after successful submission we navigate to scan page
7. Click submit again
8. Verify form progresses (navigates to scan page or shows success): `await page.waitForURL('**/scan/**', { timeout: 15000 })`

Use `test.describe('Free Scan Flow', () => { ... })` to group tests. Set reasonable timeouts — the polling + redirect flow needs extended time.
  </action>
  <verify>
Run `cd frontend && npm run build && npx playwright test free-scan.spec.ts` (requires production build first). All tests pass. If the build or testProxy has issues, inspect the test output and adjust. Verify the test covers: form filling, CFAA consent validation, scan submission, polling UI, results page content.
  </verify>
  <done>
Free scan E2E test file exists at `frontend/e2e/free-scan.spec.ts` with tests verifying the complete home-to-results journey and CFAA consent validation. Tests use mocked API responses with realistic delays.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write paid audit flow E2E test</name>
  <files>frontend/e2e/paid-audit.spec.ts</files>
  <action>
Create `frontend/e2e/paid-audit.spec.ts` importing `test, expect` from `next/experimental/testmode/playwright`.

Import fixtures from `./fixtures/results` and `./fixtures/checkout`. Import helpers from `./helpers/fetch-mocks` and `./helpers/route-mocks`.

**Test 1: "UpgradeCTA triggers Stripe Checkout redirect"**

Per locked decision: intercept the redirect and verify it targets a Stripe Checkout URL pattern.

Setup:
- `mockResultsPage(next, resultsFixtures.freeGradeB)` — results page needs data (server-side fetch)
- Mock client-side `/api/v1/checkout` POST via `page.route()`: return `checkoutFixtures.success` with 200ms delay
- Mock Stripe redirect: `page.route('https://checkout.stripe.com/**', ...)` — instead of actually navigating to Stripe, redirect to success page

Steps:
1. Navigate to results page: `await page.goto('/results/tok_e2e_abc123')` (use the token from fixtures)
2. Verify UpgradeCTA is visible: `await expect(page.locator('text=Upgrade for $49')).toBeVisible()`
3. Click the upgrade button: `await page.click('text=Upgrade for $49')`
4. Wait for button to show loading state: `await expect(page.locator('text=Redirecting to checkout...')).toBeVisible()`
5. The checkout mock returns a Stripe URL, which triggers `window.location.href = data.checkout_url`
6. Inside the `page.route('https://checkout.stripe.com/**', ...)` handler, add an explicit assertion on the intercepted URL: `expect(route.request().url()).toContain('cs_test_')` — this verifies the checkout URL targets Stripe test mode per E2E-05
7. The Stripe route mock then redirects to `/payment/success?session_id=mock_123`
8. Verify the payment success page: `await expect(page.locator('text=Payment Successful!')).toBeVisible({ timeout: 10000 })`

**Test 2: "payment success page renders correctly"**

Per locked decision: navigate directly to /payment/success?session_id=mock_123, verify success page renders.

Steps:
1. `await page.goto('/payment/success?session_id=mock_123')`
2. Verify: `await expect(page.locator('text=Payment Successful!')).toBeVisible()`
3. Verify: `await expect(page.locator('text=deep security audit is now processing')).toBeVisible()`
4. Verify return link: `await expect(page.locator('text=Return to Home')).toBeVisible()`

**Test 3: "paid tier results show enhanced content"**

Per locked decision: after payment success, navigate to results page and verify paid-tier content appears.

Setup:
- `mockResultsPage(next, resultsFixtures.paidGradeA)` — paid tier results

Steps:
1. Navigate to results page with paid fixture: `await page.goto('/results/tok_e2e_abc123')`
2. Verify grade: `await expect(page.locator('text=A')).toBeVisible()`
3. Verify UpgradeCTA is NOT visible (tier is 'paid'): `await expect(page.locator('text=Upgrade to Deep Audit')).not.toBeVisible()`
4. Verify the Download Markdown Report link exists (all tiers have this)

**Test 4: "payment cancel return is handled gracefully"**

Per locked decision: navigate to cancel/failure return URL, verify UI handles cancellation gracefully.

Stripe's cancel_url typically points back to the page where the user initiated checkout (the results page). There is no dedicated /payment/cancel page in the frontend — cancellation means the user returns to the results page.

Setup:
- `mockResultsPage(next, resultsFixtures.freeGradeB)` — results page needs data (server-side fetch)

Steps:
1. Navigate directly to the results page (simulating a Stripe cancel redirect back): `await page.goto('/results/tok_e2e_abc123')`
2. Verify the results page renders normally — not an error page: `await expect(page.locator('h1')).toContainText('Security Scan Results')`
3. Verify the UpgradeCTA is still available (user can try again): `await expect(page.locator('text=Upgrade for $49')).toBeVisible()`
4. Verify the user can navigate — "Scan again" link is visible: `await expect(page.locator('text=Scan again')).toBeVisible()`

This test confirms that when Stripe redirects back on cancellation, the user lands on a functional page with clear options to either retry the upgrade or continue using the free results.

Use `test.describe('Paid Audit Flow', () => { ... })` to group tests.

**Stripe E2E-05 note:** The Stripe test mode configuration is implicitly tested by verifying the checkout URL pattern contains `cs_test_`. No actual Stripe API keys are needed because all responses are mocked. Document the test card numbers in a comment at the top of the file for reference (e.g., `// Stripe test card: 4242 4242 4242 4242`), though they are not used in E2E tests since we mock before reaching Stripe.
  </action>
  <verify>
Run `cd frontend && npx playwright test paid-audit.spec.ts` (assumes build already exists from previous test run). All 4 tests pass. Verify: UpgradeCTA click triggers checkout, Stripe redirect is intercepted with `cs_test_` assertion, success page renders, paid-tier results hide UpgradeCTA, cancel return lands on functional results page.
  </verify>
  <done>
Paid audit E2E test file exists at `frontend/e2e/paid-audit.spec.ts` with 4 tests verifying Stripe checkout redirect interception (with explicit `cs_test_` assertion), payment success page, paid-tier content, and cancel return path handling. Stripe test mode is documented with mock checkout URLs containing `cs_test_` prefix.
  </done>
</task>

</tasks>

<verification>
1. `frontend/e2e/free-scan.spec.ts` exists with tests for full scan flow and CFAA consent
2. `frontend/e2e/paid-audit.spec.ts` exists with 4 tests for checkout redirect, success page, paid tier content, and cancel return
3. Free scan test covers: form fill → submit → scan progress → results page with grade, severity badges, findings, UpgradeCTA
4. Paid audit test covers: UpgradeCTA → checkout API → Stripe redirect intercept (with `cs_test_` assertion) → success page, plus cancel return to results page
5. All tests use mocked responses with delays in 100-500ms range
6. Tests import from `next/experimental/testmode/playwright` for `test` and `expect`
</verification>

<success_criteria>
Free scan E2E test verifies the complete user journey from home page through results, including severity badge assertions. Paid audit E2E test verifies the upgrade checkout flow through Stripe redirect (with explicit `cs_test_` URL assertion) and back to success page, plus cancel return path handling. Paid tier results correctly hide UpgradeCTA. All tests use production-like timing with mocked API responses.
</success_criteria>

<output>
After completion, create `.planning/phases/27-e2e-tests/27-02-SUMMARY.md`
</output>
