---
phase: 36-accessibility-and-touch-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/header.tsx
autonomous: true
requirements:
  - TOUCH-01
  - TOUCH-02

must_haves:
  truths:
    - "Header nav links (Dashboard, New Scan, Sign In) have a tap target of at least 44px tall"
    - "The Sign Up button has a tap target of at least 44px tall"
    - "The logo Link wrapper has expanded hit area via p-2 -m-2 so tapping near the logo still navigates home"
  artifacts:
    - path: "frontend/components/header.tsx"
      provides: "Accessible header with WCAG-compliant touch targets"
      contains: "min-h-\\[44px\\]"
  key_links:
    - from: "frontend/components/header.tsx"
      to: "nav links and logo"
      via: "Tailwind min-h-[44px] and inline-flex items-center on link anchors"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Add WCAG-minimum 44px touch targets to all interactive header elements so mobile users can tap without mis-tapping adjacent controls.

Purpose: Mobile users currently have to aim precisely at small text links. Per WCAG 2.5.5, interactive targets should be at least 44x44 CSS pixels.
Output: Updated header.tsx with min-h-[44px] on nav links/buttons and p-2 -m-2 on the logo Link.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/components/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand touch targets on header nav links and logo</name>
  <files>frontend/components/header.tsx</files>
  <action>
Update `frontend/components/header.tsx` with these specific changes:

1. **Logo Link (TOUCH-02):** Change the `<Link href="/" className="flex items-center">` to
   `<Link href="/" className="flex items-center p-2 -m-2">` so the hit area extends 8px in all directions beyond the image edge without shifting layout.

2. **Signed-in nav links (TOUCH-01):** The "Dashboard" and "New Scan" `<Link>` elements need min-h-[44px]. Change each from:
   ```
   className="text-sm font-medium text-text-secondary hover:text-text-primary transition"
   ```
   to:
   ```
   className="inline-flex items-center min-h-[44px] text-sm font-medium text-text-secondary hover:text-text-primary transition"
   ```

3. **Signed-out "Sign In" link (TOUCH-01):** Same pattern — add `inline-flex items-center min-h-[44px]`:
   ```
   className="inline-flex items-center min-h-[44px] text-sm font-medium text-text-secondary hover:text-text-primary transition"
   ```

4. **"Sign Up" button-link (TOUCH-01):** Currently has `px-4 py-2` which gives ~32px height. Replace with `px-4 min-h-[44px]` and add `inline-flex items-center`:
   ```
   className="inline-flex items-center min-h-[44px] px-4 bg-brand-primary hover:bg-brand-primary-hover text-text-inverse font-semibold rounded-lg transition text-sm sm:text-base"
   ```
   Remove `py-2` — the min-h-[44px] handles vertical sizing and centering is done by `items-center`.

Do NOT change any other classes, the sticky/z-index/border/container structure, or the responsive logo logic. Do NOT touch the UserButton (Clerk manages that component's sizing).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run --reporter=verbose __tests__/components/Header.test.tsx 2>&1 | tail -20</automated>
    <manual>Visually confirm in DevTools that each nav link/button has a computed height >= 44px (use Inspector > Computed > height). Confirm logo link has expanded hit area without shifting the header layout.</manual>
  </verify>
  <done>
All 4 existing Header tests pass. header.tsx contains min-h-[44px] on all interactive nav links/buttons and p-2 -m-2 on the logo Link wrapper.
  </done>
</task>

</tasks>

<verification>
Run the full header test suite:

```bash
cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run --reporter=verbose __tests__/components/Header.test.tsx
```

All 4 tests must pass. Grep the updated file to confirm required classes are present:

```bash
grep -n "min-h-\[44px\]\|p-2 -m-2" /home/john/vault/projects/github.com/shipsecure/frontend/components/header.tsx
```

Expected: at least 4 matches for `min-h-[44px]` (Dashboard, New Scan, Sign In, Sign Up) and 1 match for `p-2 -m-2` (logo Link).
</verification>

<success_criteria>
- `header.tsx` Logo Link has `p-2 -m-2` class on the `<Link>` wrapper
- All text nav links (Dashboard, New Scan, Sign In) have `min-h-[44px]` and `inline-flex items-center`
- Sign Up link has `min-h-[44px]` and `inline-flex items-center` (py-2 removed)
- All existing Header tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/36-accessibility-and-touch-targets/36-01-SUMMARY.md`
</output>
