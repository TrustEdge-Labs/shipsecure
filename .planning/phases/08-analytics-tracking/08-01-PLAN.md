---
phase: 08-analytics-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/app/layout.tsx
  - frontend/components/scan-form.tsx
  - frontend/app/payment/success/page.tsx
autonomous: false
user_setup:
  - service: plausible
    why: "Privacy-friendly analytics for pageviews and conversion tracking"
    account:
      - task: "Create Plausible Cloud account"
        location: "https://plausible.io/register"
    dashboard_config:
      - task: "Add shipsecure.ai as a site"
        location: "Plausible Dashboard -> + Add Website -> Domain: shipsecure.ai"
      - task: "Create Goal: Scan Submitted"
        location: "Plausible Dashboard -> shipsecure.ai -> Settings -> Goals -> + Add Goal -> Custom Event -> Name: Scan Submitted"
      - task: "Create Goal: Audit Purchased"
        location: "Plausible Dashboard -> shipsecure.ai -> Settings -> Goals -> + Add Goal -> Custom Event -> Name: Audit Purchased"

must_haves:
  truths:
    - "Every page on shipsecure.ai sends pageview data to Plausible dashboard"
    - "Submitting a free scan fires a 'Scan Submitted' custom event"
    - "Landing on the payment success page fires an 'Audit Purchased' custom event"
    - "Analytics script loads after page is interactive (afterInteractive), not blocking render"
    - "Analytics requests proxy through shipsecure.ai domain to bypass ad blockers"
  artifacts:
    - path: "frontend/app/layout.tsx"
      provides: "PlausibleProvider wrapping entire app with domain and proxy config"
      contains: "PlausibleProvider"
    - path: "frontend/next.config.ts"
      provides: "Plausible proxy rewrite rules via withPlausibleProxy"
      contains: "withPlausibleProxy"
    - path: "frontend/components/scan-form.tsx"
      provides: "Scan Submitted event firing on successful scan creation"
      contains: "Scan Submitted"
    - path: "frontend/app/payment/success/page.tsx"
      provides: "Audit Purchased event firing on page load"
      contains: "Audit Purchased"
    - path: "frontend/package.json"
      provides: "next-plausible dependency"
      contains: "next-plausible"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "plausible.io (proxied)"
      via: "PlausibleProvider script injection"
      pattern: "PlausibleProvider.*domain.*shipsecure\\.ai"
    - from: "frontend/next.config.ts"
      to: "plausible.io"
      via: "withPlausibleProxy rewrite rules"
      pattern: "withPlausibleProxy"
    - from: "frontend/components/scan-form.tsx"
      to: "plausible event API"
      via: "usePlausible hook called when scanId is received"
      pattern: "plausible\\(.*Scan Submitted"
    - from: "frontend/app/payment/success/page.tsx"
      to: "plausible event API"
      via: "usePlausible hook called on mount"
      pattern: "plausible\\(.*Audit Purchased"
---

<objective>
Integrate Plausible Analytics into the ShipSecure Next.js frontend for privacy-friendly pageview tracking and conversion event tracking.

Purpose: Phase 8 provides analytics visibility into user behavior before the Hacker News launch — tracking which pages get traffic and measuring scan submission and paid audit conversion rates. This data informs all subsequent v1.2 optimization phases (SEO, UX, Landing Page).

Output: All pages automatically track pageviews via PlausibleProvider. Two custom events fire for key conversions: "Scan Submitted" (free scan created) and "Audit Purchased" (payment success page loaded). Analytics requests proxy through the app domain to bypass ad blockers.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analytics-tracking/08-RESEARCH.md
@frontend/app/layout.tsx
@frontend/next.config.ts
@frontend/package.json
@frontend/components/scan-form.tsx
@frontend/app/payment/success/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-plausible, configure PlausibleProvider and proxy</name>
  <files>
    frontend/package.json
    frontend/next.config.ts
    frontend/app/layout.tsx
  </files>
  <action>
    1. Install next-plausible:
       ```
       cd frontend && npm install next-plausible
       ```

    2. Update `frontend/next.config.ts` to wrap the existing config with `withPlausibleProxy()`.
       The current config is TypeScript and uses `NextConfig` type. The updated file should:
       - Import `withPlausibleProxy` from `next-plausible`
       - Wrap the existing config object: `export default withPlausibleProxy()({ output: 'standalone' })`
       - Keep the TypeScript type annotation working (withPlausibleProxy returns a NextConfig-compatible object)
       - NOTE: `withPlausibleProxy()` uses ES module import. Since the file is `.ts`, use `import` syntax (not `require`).

    3. Update `frontend/app/layout.tsx` to add PlausibleProvider inside the `<head>` element:
       - Import `PlausibleProvider` from `next-plausible`
       - Add `<PlausibleProvider domain="shipsecure.ai" trackOutboundLinks />` inside `<head>` before `</head>`
       - The existing `<html lang="en">` does NOT have a `<head>` tag currently, so add one: `<head><PlausibleProvider ... /></head>`
       - Keep all existing code (Inter font, metadata export, body classes) unchanged
       - Do NOT add `trackLocalhost` — that is for dev only and should not ship to production

    4. Verify the build succeeds:
       ```
       cd frontend && npm run build
       ```
  </action>
  <verify>
    - `npm run build` in frontend/ completes without errors
    - `grep "next-plausible" frontend/package.json` shows the dependency
    - `grep "PlausibleProvider" frontend/app/layout.tsx` shows the provider
    - `grep "withPlausibleProxy" frontend/next.config.ts` shows the proxy config
  </verify>
  <done>
    next-plausible is installed. PlausibleProvider wraps the app with domain="shipsecure.ai" and trackOutboundLinks enabled. next.config.ts proxies analytics requests through the app domain. Frontend builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add custom conversion events to scan form and payment success page</name>
  <files>
    frontend/components/scan-form.tsx
    frontend/app/payment/success/page.tsx
  </files>
  <action>
    1. Update `frontend/components/scan-form.tsx` to fire "Scan Submitted" event when a scan is successfully created:
       - Import `usePlausible` from `next-plausible`
       - Call `const plausible = usePlausible()` inside the ScanForm component
       - Fire the event INSIDE the existing `useEffect` that runs when `state.scanId` is truthy (this means the scan was successfully created, not just that the form was submitted). Add `plausible('Scan Submitted', { props: { scan_type: 'url' } })` as the first line of that useEffect callback, before the setTimeout
       - This ensures the event only fires on successful scan creation, not on validation errors
       - Do NOT fire the event on form submit (the form uses useActionState with a server action — the submit itself is not the right moment)

    2. Update `frontend/app/payment/success/page.tsx` to fire "Audit Purchased" event on page load:
       - This is currently a Server Component (no 'use client' directive). It needs to become a Client Component OR use a small client component for the event.
       - Preferred approach: Add a small client component inline or extract to a separate file. Since this page is simple and has no server-side data fetching, convert the whole page to a client component:
         - Add `'use client'` at the top
         - Import `usePlausible` from `next-plausible` and `useEffect` from `react`
         - Inside the component, call `const plausible = usePlausible()`
         - Add a `useEffect(() => { plausible('Audit Purchased', { props: { value: '49', currency: 'USD' } }) }, [])` that fires once on mount
       - Keep all existing JSX and styling unchanged
       - The useEffect empty dependency array ensures it fires exactly once on page load

    Event naming convention: Use the exact strings "Scan Submitted" and "Audit Purchased" — these must match the Goal names created in the Plausible dashboard. Property keys use lowercase_underscores.
  </action>
  <verify>
    - `npm run build` in frontend/ completes without errors (ensures no TypeScript issues with client component conversion)
    - `grep "Scan Submitted" frontend/components/scan-form.tsx` shows the event
    - `grep "Audit Purchased" frontend/app/payment/success/page.tsx` shows the event
    - `grep "usePlausible" frontend/components/scan-form.tsx` shows the hook import
    - `grep "use client" frontend/app/payment/success/page.tsx` shows client directive
  </verify>
  <done>
    "Scan Submitted" event fires when a free scan is successfully created (scanId received from server action). "Audit Purchased" event fires once when the payment success page loads. Both events include minimal, non-PII properties. Frontend builds successfully with both changes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plausible Analytics integration with pageview tracking and two custom conversion events (Scan Submitted, Audit Purchased). Analytics requests proxy through the app domain to bypass ad blockers.
  </what-built>
  <how-to-verify>
    Pre-requisite: You need a Plausible Cloud account with shipsecure.ai added as a site and two Goals created ("Scan Submitted" and "Audit Purchased").

    1. Deploy the updated frontend to production (rebuild Docker image, push to GHCR, pull on server)
    2. Visit https://shipsecure.ai in a browser
    3. Open browser DevTools -> Network tab -> filter by "plausible" or "api/event"
       - Confirm you see a request to shipsecure.ai (proxied, NOT plausible.io directly)
       - The response should be 202 Accepted
    4. Check Plausible dashboard (https://plausible.io/shipsecure.ai) — you should see your pageview appear in real-time
    5. Submit a free scan on the landing page
       - After successful submission (green "Scan started!" message), check Network tab for a "Scan Submitted" event request
       - Check Plausible dashboard -> Goals for the event
    6. (Optional) If you can trigger a test payment flow, verify "Audit Purchased" fires on the payment success page
    7. Run Lighthouse on https://shipsecure.ai — Performance score should remain >90 (analytics should not degrade it)
  </how-to-verify>
  <resume-signal>Type "approved" if analytics are working correctly, or describe any issues you observe.</resume-signal>
</task>

</tasks>

<verification>
Phase 8 verification checks:

1. **ANLYT-01 (Pageview tracking):**
   - PlausibleProvider exists in layout.tsx with domain="shipsecure.ai"
   - withPlausibleProxy configured in next.config.ts
   - All pages inherit the provider (it wraps the root layout)
   - Pageviews appear in Plausible dashboard

2. **ANLYT-02 (Custom conversion events):**
   - "Scan Submitted" event fires in scan-form.tsx on successful scan creation
   - "Audit Purchased" event fires in payment/success/page.tsx on mount
   - Events appear in Plausible dashboard Goals section

3. **Performance (non-blocking):**
   - PlausibleProvider uses afterInteractive strategy by default (built into next-plausible)
   - Script is <1KB
   - Lighthouse Performance score unchanged
</verification>

<success_criteria>
- Plausible dashboard shows real-time pageviews for shipsecure.ai
- "Scan Submitted" and "Audit Purchased" events appear in Plausible Goals
- Analytics requests route through shipsecure.ai domain (proxy working)
- Lighthouse Performance score >90 on mobile (analytics do not degrade performance)
- Frontend builds and deploys without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics-tracking/08-01-SUMMARY.md`
</output>
