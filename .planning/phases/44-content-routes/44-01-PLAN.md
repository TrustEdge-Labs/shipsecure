---
phase: 44-content-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/next.config.ts
  - frontend/mdx-components.tsx
  - frontend/app/blog/page.tsx
  - frontend/app/blog/[slug]/page.tsx
  - frontend/app/blog/blog.css
  - content/blog/.gitkeep
autonomous: true
requirements: [CONTENT-01, CONTENT-02]
must_haves:
  truths:
    - "/blog shows a coming-soon page with scan CTA when no published posts exist"
    - "/blog lists published posts sorted by date descending when posts exist"
    - "/blog/[slug] renders an MDX post with Geist typography and article time element"
  artifacts:
    - path: "frontend/next.config.ts"
      provides: "MDX plugin registration"
      contains: "@next/mdx"
    - path: "frontend/mdx-components.tsx"
      provides: "MDX component mapping with typography styles"
      exports: ["useMDXComponents"]
    - path: "frontend/app/blog/page.tsx"
      provides: "Blog index route with coming-soon fallback"
      contains: "Coming soon"
    - path: "frontend/app/blog/[slug]/page.tsx"
      provides: "Individual blog post route with generateMetadata"
      contains: "generateMetadata"
    - path: "content/blog/.gitkeep"
      provides: "Blog content directory placeholder"
  key_links:
    - from: "frontend/next.config.ts"
      to: "@next/mdx"
      via: "MDX plugin config"
      pattern: "createMDX|@next/mdx"
    - from: "frontend/app/blog/[slug]/page.tsx"
      to: "content/blog/"
      via: "dynamic import of MDX files"
      pattern: "import.*content/blog"
---

<objective>
Set up MDX blog infrastructure and /blog routes so the site has a content marketing surface.

Purpose: CONTENT-01 and CONTENT-02 -- blog route that renders MDX posts with proper typography, and shows a "coming soon" page with scan CTA when no posts exist yet.
Output: Working /blog and /blog/[slug] routes, MDX compilation via @next/mdx, Geist-styled prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-content-routes/44-CONTEXT.md
@DESIGN.md
@frontend/next.config.ts
@frontend/app/page.tsx
@frontend/components/page-container.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/components/page-container.tsx:
```typescript
interface PageContainerProps {
  children: ReactNode
  maxWidth?: string
  className?: string
}
export function PageContainer({ children, maxWidth, className }: PageContainerProps)
```

From frontend/app/page.tsx (metadata pattern):
```typescript
export const metadata: Metadata = {
  title: '...',
  description: '...',
  openGraph: { ... },
  twitter: { ... },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @next/mdx and configure MDX compilation</name>
  <files>frontend/next.config.ts, frontend/package.json, frontend/mdx-components.tsx, frontend/app/blog/blog.css, content/blog/.gitkeep</files>
  <read_first>
    - frontend/next.config.ts (current config to extend with MDX)
    - DESIGN.md (typography specs: Geist 400 body 16px/1.6, Geist Mono for code, color tokens)
    - frontend/app/globals.css (existing CSS variable definitions)
  </read_first>
  <action>
    1. Install @next/mdx and @mdx-js/react: `cd frontend && npm install @next/mdx @mdx-js/react` (per D-05, minimal MDX setup, no CMS).

    2. Update `frontend/next.config.ts`:
       - Import createMDX from `@next/mdx`
       - Wrap the existing nextConfig with `createMDX()(nextConfig)`
       - Add `pageExtensions: ['ts', 'tsx', 'md', 'mdx']` to nextConfig
       - Preserve ALL existing config (CSP headers, output: 'standalone', experimental)

    3. Create `frontend/mdx-components.tsx` (required by @next/mdx):
       - Export `useMDXComponents` function
       - Map h1/h2/h3 to styled heading elements using Geist typography from DESIGN.md:
         - h1: text-4xl font-bold text-text-primary (48px per DESIGN.md hero scale)
         - h2: text-2xl font-semibold text-text-primary (32px per DESIGN.md)
         - h3: text-lg font-semibold text-text-primary (18px per DESIGN.md)
       - Map `p` to text-text-secondary leading-relaxed (body 16px/1.6)
       - Map `a` to text-brand-primary hover:underline
       - Map `code` to font-mono text-sm bg-surface-elevated px-1.5 py-0.5 rounded (Geist Mono per DESIGN.md)
       - Map `pre` to bg-surface-elevated p-4 rounded-lg overflow-x-auto border border-border-default
       - Map `ul`/`ol` to list-disc/list-decimal with left padding and text-text-secondary
       - Map `blockquote` to border-l-2 border-brand-primary pl-4 italic text-text-secondary

    4. Create `frontend/app/blog/blog.css` with article-specific typography overrides:
       - `.blog-article` wrapper class with max-width 720px, margin auto
       - Spacing between elements: `> * + * { margin-top: 1.5rem; }`
       - Code block: Geist Mono font-family

    5. Create `content/blog/.gitkeep` to establish the blog content directory (per D-01, content lives at project root, not inside app/).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/shipsecure/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/next.config.ts contains "createMDX" or "@next/mdx"
    - frontend/mdx-components.tsx exports "useMDXComponents"
    - frontend/app/blog/blog.css contains "blog-article"
    - content/blog/.gitkeep exists
    - `npm run build` in frontend succeeds without errors
  </acceptance_criteria>
  <done>MDX compilation configured, typography components mapped, blog content directory created. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create /blog index and /blog/[slug] routes</name>
  <files>frontend/app/blog/page.tsx, frontend/app/blog/[slug]/page.tsx</files>
  <read_first>
    - frontend/app/page.tsx (hero + ScanForm pattern, metadata pattern)
    - frontend/components/page-container.tsx (PageContainer usage)
    - frontend/mdx-components.tsx (just created in Task 1)
    - .planning/phases/44-content-routes/44-CONTEXT.md (D-02 through D-05 for blog decisions)
  </read_first>
  <action>
    1. Create `frontend/app/blog/page.tsx` (blog index, per D-03):
       - Server component with `generateMetadata`: title "Blog | ShipSecure", description "Security research and vibe-code vulnerability analysis."
       - Read `content/blog/` directory using `fs.readdirSync` (Node fs, server component)
       - For each `.mdx` file: dynamically import it, extract frontmatter from the module's exported `metadata` object (per D-02: title, date, slug, description, published)
       - Filter to `published: true` only, sort by date descending
       - If no published posts: render "coming soon" page (per D-03):
         - Use PageContainer maxWidth="max-w-2xl"
         - H1: "Blog" in text-4xl font-bold text-text-primary
         - P: "Security research and vibe-code vulnerability analysis. Coming soon." in text-text-secondary
         - Link to "/" with scan CTA: "Scan your app now" styled as primary button (bg-brand-primary text-white rounded-lg px-6 py-3 inline-block)
       - If posts exist: render list with title, date (formatted as "Mar 29, 2026"), description, link to /blog/[slug]

    2. Create `frontend/app/blog/[slug]/page.tsx` (individual post, per D-04):
       - Server component with dynamic `generateMetadata` from post frontmatter (title, description, openGraph)
       - Use `generateStaticParams` to list all MDX files in content/blog/
       - Dynamic import: `const Post = (await import(\`@/../../content/blog/${params.slug}.mdx\`)).default`
       - If slug not found: call `notFound()` from next/navigation
       - Render with PageContainer maxWidth="max-w-3xl"
       - Wrap in `<article>` with blog-article CSS class (from blog.css)
       - Import blog.css at the top
       - Include `<time dateTime="2026-03-29">` element with formatted date (per D-04, "Mar 29, 2026" format)
       - Include back-link to /blog at bottom: "Back to blog" in text-text-secondary hover:text-brand-primary

    NOTE: The MDX frontmatter approach -- each .mdx file should export a `metadata` const at the top:
    ```
    export const metadata = {
      title: 'Post Title',
      date: '2026-03-29',
      slug: 'post-slug',
      description: 'Post description',
      published: true,
    }
    ```
    This is the @next/mdx pattern (not gray-matter parsing). The slug matches the filename.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/shipsecure/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/blog/page.tsx contains "Coming soon" and "Scan your app"
    - frontend/app/blog/[slug]/page.tsx contains "generateMetadata" and "article"
    - frontend/app/blog/[slug]/page.tsx contains "<time" element
    - `npm run build` succeeds and /blog route is listed in build output
  </acceptance_criteria>
  <done>/blog shows coming-soon page with scan CTA (no published posts exist yet). /blog/[slug] route is ready to render any MDX post added to content/blog/. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes without errors
2. Build output shows /blog route
3. `grep -r "Coming soon" frontend/app/blog/page.tsx` returns a match
4. `grep -r "generateMetadata" frontend/app/blog/\[slug\]/page.tsx` returns a match
</verification>

<success_criteria>
- /blog route exists and renders coming-soon content with scan CTA (CONTENT-02)
- /blog/[slug] route exists and can render MDX posts with Geist typography (CONTENT-01)
- MDX compilation configured via @next/mdx in next.config.ts
- Blog content directory at content/blog/ (per D-01)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/44-content-routes/44-01-SUMMARY.md`
</output>
