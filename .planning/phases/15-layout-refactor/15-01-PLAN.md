---
phase: 15-layout-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "CSS variable --header-height is defined as 64px and available globally via @theme inline"
    - "All existing routes render identically before and after the variable addition (no visual change)"
    - "The layout structure is prepared for Phase 16 header insertion without requiring further layout refactoring"
    - "Next.js build succeeds with zero errors after changes"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Layout dimension token --header-height: 64px in @theme inline block"
      contains: "--header-height: 64px"
    - path: "frontend/app/layout.tsx"
      provides: "Layout structure with documented header slot for Phase 16"
      contains: "header"
  key_links:
    - from: "frontend/app/globals.css"
      to: "frontend/app/layout.tsx"
      via: "@theme inline variable available as Tailwind utility"
      pattern: "--header-height"
---

<objective>
Define the `--header-height: 64px` CSS custom property in the design token system and prepare the root layout for Phase 16 header integration, without changing any visual output on existing routes.

Purpose: Phase 16 will add a sticky header. Defining the height token now and documenting the insertion point in layout.tsx prevents layout shift when the header is added, because the variable already exists as a single source of truth.
Output: Updated globals.css with layout dimension token, updated layout.tsx with header slot comment, verified build.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-layout-refactor/15-RESEARCH.md
@frontend/app/globals.css
@frontend/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define header-height layout token and prepare layout structure</name>
  <files>frontend/app/globals.css, frontend/app/layout.tsx</files>
  <action>
In `frontend/app/globals.css`, add a "Layout Dimensions" section inside the existing `@theme inline` block (after the last token, before the closing brace). Add:

```css
  /* ===================================================================
     Layout Dimensions
     =================================================================== */

  /* Header height - reserved for Phase 16 sticky header.
   * Used for: sticky header height, main content padding-top offset.
   * DO NOT change without verifying all routes maintain spacing. */
  --header-height: 64px;
```

IMPORTANT: Add this inside the LIGHT MODE `@theme inline` block only (lines ~114-209). Do NOT add it inside the dark mode `@media (prefers-color-scheme: dark)` block -- layout dimensions do not change between color modes.

In `frontend/app/layout.tsx`, add a comment marking the header insertion point for Phase 16. Update the body content structure to:

```tsx
<div className="flex flex-col min-h-screen">
  {/* Phase 16: Insert <Header /> sticky component here */}
  <div className="flex-1">
    {children}
  </div>
  <Footer />
</div>
```

Do NOT add any spacer div, padding, or visual changes. The current Flexbox layout stays exactly as-is. The comment is the only change to layout.tsx -- it documents where the header goes. Do NOT migrate to CSS Grid in this phase (that is deferred to Phase 16 per research recommendation).

Why no spacer/padding: Adding `pt-[--header-height]` or a spacer div before the header exists would add 64px of unexplained whitespace to every page, violating the "no layout shift" success criterion.
  </action>
  <verify>
Run `cd /home/john/vault/projects/github.com/trustedge-audit/frontend && npx next build` -- build must succeed with zero errors.

Verify the token is defined: `grep -- '--header-height: 64px' frontend/app/globals.css` returns a match.

Verify the header slot comment exists: `grep 'Phase 16.*Header' frontend/app/layout.tsx` returns a match.
  </verify>
  <done>
globals.css contains `--header-height: 64px` inside `@theme inline` block with documentation comment. layout.tsx contains Phase 16 header insertion point comment. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all routes maintain current spacing with no layout shift</name>
  <files>frontend/app/globals.css</files>
  <action>
After the token is defined, verify no existing routes are affected by running a comprehensive check:

1. Run `npx next build` in the frontend directory to confirm the full build passes (TypeScript compilation, CSS processing, all routes pre-render successfully).

2. Grep the codebase for any existing usage of `--header-height` that might conflict:
   - `grep -r "header-height" frontend/` should return ONLY the globals.css definition and the layout.tsx comment.
   - If any other file references `--header-height`, investigate whether the token addition changes behavior.

3. Verify that no page uses `h-screen` (which would conflict with a future sticky header):
   - Confirm that pages use `min-h-screen` (which is safe) rather than `h-screen` (which is not).
   - The current codebase uses `min-h-screen` throughout, which is correct. Document this finding.

4. Verify the token is accessible as a Tailwind utility by checking the build output includes the CSS variable. The `@theme inline` directive automatically generates utility support, so `h-[--header-height]` will work in Phase 16 without additional configuration.

5. Confirm that `global-error.tsx` (which uses inline `minHeight: '100vh'` style) is unaffected since it renders outside the root layout and does not reference the header height token.

No files should be modified in this task -- it is purely verification. If any issue is found, fix it in globals.css and re-verify.
  </action>
  <verify>
`npx next build` succeeds. `grep -r "header-height" frontend/` returns only globals.css and layout.tsx matches. No pages use `h-screen` (only `min-h-screen`).
  </verify>
  <done>
Build passes, no routes affected, no conflicting usage found, all pages use safe `min-h-screen` pattern. Phase 15 layout preparation is complete and ready for Phase 16 header integration.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` in frontend/ succeeds with zero errors
2. `grep -- '--header-height: 64px' frontend/app/globals.css` returns exactly one match inside `@theme inline`
3. `grep 'Phase 16' frontend/app/layout.tsx` returns the header insertion comment
4. `grep -r 'header-height' frontend/ --include='*.tsx' --include='*.css'` returns only globals.css and layout.tsx
5. No visual changes on any route (token is defined but not yet consumed by any component)
</verification>

<success_criteria>
- CSS variable `--header-height: 64px` is defined in globals.css `@theme inline` block
- layout.tsx has documented header insertion point for Phase 16
- Next.js build passes with zero errors
- No existing route spacing is affected (token defined but unused)
- No `h-screen` conflicts exist in the codebase (all pages use safe `min-h-screen`)
</success_criteria>

<output>
After completion, create `.planning/phases/15-layout-refactor/15-01-SUMMARY.md`
</output>
