---
phase: 12-landing-page-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/page.tsx
  - frontend/components/footer.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page headline states product purpose (security scanning) and target audience (AI-generated web apps)"
    - "How it works section explains 3-step scan process with methodology disclosure"
    - "Footer credits Nuclei (MIT, ProjectDiscovery) and testssl.sh (GPLv2) with links"
    - "All copy avoids marketing superlatives and vague promises"
    - "Feature descriptions list specific vulnerability types instead of vague benefits"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Developer-focused landing page with hero, how-it-works, and enhanced feature cards"
      contains: "Security scanning for AI-generated web apps"
    - path: "frontend/components/footer.tsx"
      provides: "Footer with OSS attribution section"
      contains: "Nuclei"
  key_links:
    - from: "frontend/components/footer.tsx"
      to: "https://github.com/projectdiscovery/nuclei"
      via: "external link in attribution"
      pattern: "projectdiscovery/nuclei"
    - from: "frontend/components/footer.tsx"
      to: "https://testssl.sh"
      via: "external link in attribution"
      pattern: "testssl\\.sh"
---

<objective>
Rewrite landing page for developer-focused technical honesty and add methodology transparency and open-source attribution.

Purpose: Replace vague marketing copy ("Ship fast, stay safe") with clear product description targeting developers using AI code generation. Add "How it works" methodology section for transparency. Credit open-source tools in footer.
Output: Updated landing page copy (page.tsx) and footer with OSS attribution (footer.tsx)
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-landing-page-optimization/12-RESEARCH.md

@frontend/app/page.tsx
@frontend/components/footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite landing page hero, features, and add "How It Works" section</name>
  <files>frontend/app/page.tsx</files>
  <action>
Rewrite the landing page copy in frontend/app/page.tsx. Three changes:

**1. Hero Section — Replace vague headline with developer-focused copy:**

Replace:
- H1: "Ship fast, stay safe." -> "Security scanning for AI-generated web apps"
- Subhead: "Free security scanning for vibe-coded web apps." -> "Free vulnerability detection for vibe-coded projects. Detects exposed .env files, weak TLS ciphers, hardcoded API keys, and framework-specific misconfigurations."
- Small text: "Catch security flaws before they become breaches." -> "No signup required. Results in ~60 seconds."

Keep the existing gradient text styling on the h1. Keep the existing Tailwind classes for responsive sizing.

**2. "What We Check" Section — Make descriptions more specific:**

Update the four feature card descriptions to be more technically specific:
- Security Headers: Change "CSP, HSTS, X-Frame-Options, and more" -> "Analyzes CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy"
- TLS Configuration: Change "Certificate validity, protocol versions, cipher strength" -> "Certificate chain validation, TLS 1.2/1.3 protocol versions, cipher suite strength via SSL Labs API"
- Exposed Files: Change ".env, .git, config files, and sensitive data" -> "Probes for .env, .git/config, /debug, /admin, wp-config.php, and 20+ sensitive paths"
- JavaScript Secrets: Change "API keys, tokens, and credentials in client code" -> "Pattern-matches bundled JS for AWS keys, Stripe tokens, Firebase credentials, and API secrets"

**3. Add "How It Works" Section — Insert between "What We Check" and social proof:**

Add a new section after the "What We Check" div and before the social proof div. Structure:

```
<h2>How it works</h2>
<3-column grid (stacks on mobile) with numbered steps:>

Step 1: "Submit your URL"
  "Enter any URL. No signup, no API key. Scans start immediately."

Step 2: "Automated analysis"
  "Four scanners run in parallel: HTTP header analysis, TLS configuration check via SSL Labs, sensitive file probing, and JavaScript static analysis."

Step 3: "Prioritized results"
  "Get an A-F security grade with severity-ranked findings and copy-paste remediation steps. Paid tier adds Nuclei-powered framework-specific checks."

<details> element with "Scan methodology" summary:
  - Security headers: Passive HTTP response header analysis (CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)
  - TLS configuration: Qualys SSL Labs API — certificate chain, protocol versions, cipher strength, known vulnerabilities
  - Exposed files: Active HTTP probes for common sensitive paths (.env, .git/config, /debug, /admin, backup files)
  - JavaScript secrets: Regex pattern matching on bundled JavaScript for API keys, tokens, and credentials
  - Vibe-code scanning (paid): Nuclei with custom templates for Supabase RLS bypass, Firebase security rules, Next.js/React misconfigurations
```

Use the same Tailwind styling patterns as the existing page: text-gray-600/400 for body text, text-gray-900/100 for headings, text-blue-600/400 for accents. Use `text-center` for the section heading. Use `grid md:grid-cols-3 gap-6` for the 3-step layout. Number indicators should use `text-3xl font-bold text-blue-600 dark:text-blue-400`. The details element should use `text-sm text-gray-600 dark:text-gray-400` with a `cursor-pointer hover:text-blue-600` summary. Add `mb-12` to the section wrapper.

Do NOT change: the metadata export, the getScanCount function, the JSON-LD schemas, the ScanForm component, or the social proof section logic.

Update the metadata description to match new copy: "Free vulnerability detection for AI-generated web apps. Detects exposed .env files, weak TLS, hardcoded API keys, and framework misconfigurations. No signup required."

Also update the openGraph and twitter description fields to match.
  </action>
  <verify>
Run `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build 2>&1 | tail -20` to confirm the build succeeds with no errors.

Verify the new content by checking:
- `grep "Security scanning for AI-generated" frontend/app/page.tsx` returns the new headline
- `grep "How it works" frontend/app/page.tsx` returns the new section heading
- `grep "Scan methodology" frontend/app/page.tsx` returns the details summary
- `grep "No signup required" frontend/app/page.tsx` returns the updated small text
  </verify>
  <done>
Landing page headline reads "Security scanning for AI-generated web apps". Subhead lists specific vulnerability types. "How it works" 3-step section explains scan process. Technical methodology details available via expandable details element. All copy is technically honest with no marketing superlatives. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add open-source attribution to footer</name>
  <files>frontend/components/footer.tsx</files>
  <action>
Add an open-source attribution section to the footer component in frontend/components/footer.tsx.

Add below the existing copyright paragraph, with `mt-4` spacing:

```
<div className="text-center text-xs text-gray-400 dark:text-gray-500">
  <p className="mb-1">Powered by open source:</p>
  <div className="flex flex-wrap justify-center gap-x-3 gap-y-1">
    <span>
      <a href="https://github.com/projectdiscovery/nuclei" target="_blank" rel="noopener noreferrer" className="hover:text-blue-600 dark:hover:text-blue-400 underline">Nuclei</a>
      {' '}by{' '}
      <a href="https://projectdiscovery.io" target="_blank" rel="noopener noreferrer" className="hover:text-blue-600 dark:hover:text-blue-400">ProjectDiscovery</a>
      {' '}(<a href="https://github.com/projectdiscovery/nuclei/blob/main/LICENSE.md" target="_blank" rel="noopener noreferrer" className="hover:text-blue-600 dark:hover:text-blue-400">MIT</a>)
    </span>
    <span className="hidden sm:inline text-gray-400 dark:text-gray-600">·</span>
    <span>
      <a href="https://testssl.sh" target="_blank" rel="noopener noreferrer" className="hover:text-blue-600 dark:hover:text-blue-400 underline">testssl.sh</a>
      {' '}(<a href="https://github.com/testssl/testssl.sh/blob/3.3dev/LICENSE" target="_blank" rel="noopener noreferrer" className="hover:text-blue-600 dark:hover:text-blue-400">GPLv2</a>)
    </span>
  </div>
</div>
```

The attribution follows the Title + Author + Source + License pattern from OSS attribution best practices. Use the same hover styling pattern as the legal links (hover:text-blue-600). Use `hidden sm:inline` for the separator dot (matching the legal links separator pattern). The text is smaller (text-xs) and lighter (text-gray-400) than the copyright to be visible but not dominant.

Do NOT change: the existing nav with Privacy Policy and Terms of Service links, or the copyright line.
  </action>
  <verify>
Run `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build 2>&1 | tail -20` to confirm build succeeds.

Verify attribution content:
- `grep "Nuclei" frontend/components/footer.tsx` returns the Nuclei attribution
- `grep "testssl.sh" frontend/components/footer.tsx` returns the testssl attribution
- `grep "ProjectDiscovery" frontend/components/footer.tsx` returns the author credit
- `grep "MIT" frontend/components/footer.tsx` returns the license link
- `grep "GPLv2" frontend/components/footer.tsx` returns the license link
  </verify>
  <done>
Footer displays "Powered by open source:" followed by Nuclei (with ProjectDiscovery author credit and MIT license link) and testssl.sh (with GPLv2 license link). All links open in new tabs. Attribution is visible on all pages via the shared footer component. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build` completes without errors
2. Landing page hero headline contains "Security scanning for AI-generated web apps"
3. Landing page contains "How it works" section with 3 numbered steps
4. Landing page contains expandable "Scan methodology" details element
5. Footer contains Nuclei attribution with ProjectDiscovery author and MIT license link
6. Footer contains testssl.sh attribution with GPLv2 license link
7. No marketing superlatives exist in page copy (grep for "revolutionary", "game-changing", "enterprise-grade", "10x")
8. All external links in footer use target="_blank" rel="noopener noreferrer"
</verification>

<success_criteria>
- Landing page headline clearly states product purpose and target audience (LAND-01)
- "How it works" section explains scan methodology in 3 steps with expandable technical details (LAND-02)
- Footer credits Nuclei (MIT, ProjectDiscovery) and testssl.sh (GPLv2) with proper links (LAND-03)
- All copy is technically honest — no superlatives, vague promises, or marketing jargon (LAND-01)
- Frontend builds successfully with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-landing-page-optimization/12-01-SUMMARY.md`
</output>
