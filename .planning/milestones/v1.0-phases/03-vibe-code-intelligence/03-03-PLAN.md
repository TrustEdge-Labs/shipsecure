---
phase: 03-vibe-code-intelligence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scanners/remediation.rs
  - src/scanners/mod.rs
autonomous: true

must_haves:
  truths:
    - "Remediation for Next.js env leaks includes targeted diff to remove NEXT_PUBLIC_ prefix"
    - "Remediation for unprotected routes includes copy-paste middleware.ts code for Next.js"
    - "Remediation for Supabase RLS includes SQL to enable RLS on specific tables"
    - "Remediation for Firebase includes JSON rules restricting read/write"
    - "Remediation includes 1-2 sentence explanation of why the fix works"
    - "Generic fallback remediation exists when no framework detected"
  artifacts:
    - path: "src/scanners/remediation.rs"
      provides: "Framework-specific remediation generation"
      contains: "generate_remediation"
      exports: ["generate_remediation"]
  key_links:
    - from: "src/scanners/remediation.rs"
      to: "src/models/detection.rs"
      via: "Framework enum matching"
      pattern: "Framework"
---

<objective>
Build the remediation engine that generates copy-paste code fixes tailored to the detected framework.

Purpose: Per user decision, remediation must be "targeted diffs" with "code block + 1-2 sentence explanation." This is the module that transforms a generic "env variable leaked" finding into "In your .env.local, remove the NEXT_PUBLIC_ prefix" with exact code. No "verify your fix" sections -- users rescan to verify.

Output: Remediation module with framework-specific fix generation for all vibe-code vulnerability types.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vibe-code-intelligence/03-CONTEXT.md
@.planning/phases/03-vibe-code-intelligence/03-RESEARCH.md
@src/models/finding.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Framework-specific remediation engine</name>
  <files>
    src/scanners/remediation.rs
    src/scanners/mod.rs
  </files>
  <action>
1. Add `pub mod remediation;` to `src/scanners/mod.rs`.

2. Create `src/scanners/remediation.rs` with the remediation generation engine.

Define vulnerability types as an enum (internal to this module, not exposed):
```
enum VulnType {
    EnvLeak,           // NEXT_PUBLIC_ secrets, .env exposure
    SupabaseRls,       // RLS disabled on sensitive tables
    FirebaseRules,     // Permissive Firebase rules
    UnprotectedRoute,  // API routes without auth
    NetlifyExposure,   // Netlify function/config exposure
    VercelEnvLeak,     // Vercel deployment info leak
    Generic,           // Unknown vulnerability type
}
```

Implement `fn classify_vuln(template_id: &str, title: &str) -> VulnType`:
- Match on Nuclei template IDs from our custom templates: "supabase-rls" -> SupabaseRls, "firebase-rules" -> FirebaseRules, "nextjs-env-leak" -> EnvLeak, "unprotected-api-routes" -> UnprotectedRoute, "env-in-build-output" -> EnvLeak, "netlify-function-exposure" -> NetlifyExposure, "vercel-env-leak" -> VercelEnvLeak
- Fallback to title keyword matching if template ID doesn't match

Implement `pub fn generate_remediation(template_id: &str, title: &str, framework: Option<&str>, raw_evidence: Option<&str>) -> String`:
- Classify the vulnerability type
- Dispatch to framework-specific remediation based on (vuln_type, framework) tuple
- Return formatted string with code block + explanation
- `raw_evidence` is optional context from the Nuclei finding (e.g., matched URL, extracted values)

Framework-specific remediation content (these are the actual copy-paste fixes):

**EnvLeak + nextjs:**
```
**In your `.env.local` file:**
\```diff
- NEXT_PUBLIC_{VAR_NAME}=...
+ {VAR_NAME}=...  # Remove NEXT_PUBLIC_ prefix
\```

**In your server-side code (`app/api/route.ts` or `pages/api/*.ts`):**
\```typescript
const secret = process.env.{VAR_NAME};
\```

Variables prefixed with `NEXT_PUBLIC_` are embedded in client-side JavaScript bundles and visible to anyone. Secrets must only exist server-side. **Rotate the exposed secret immediately.**
```

**EnvLeak + sveltekit:**
```
**In your `.env` file:**
\```diff
- PUBLIC_{VAR_NAME}=...
+ {VAR_NAME}=...  # Remove PUBLIC_ prefix
\```

**In your server-side code (`+page.server.ts` or `+server.ts`):**
\```typescript
import { env } from '$env/static/private';
const secret = env.{VAR_NAME};
\```

SvelteKit exposes variables prefixed with `PUBLIC_` to the browser. Remove the prefix and access via `$env/static/private` for server-only values.
```

**EnvLeak + nuxt:**
```
**In your `nuxt.config.ts`:**
\```diff
export default defineNuxtConfig({
  runtimeConfig: {
-   public: {
-     secretKey: process.env.SECRET_KEY
-   }
+   secretKey: process.env.SECRET_KEY  // Server-only (not under public)
  }
})
\```

In Nuxt, values under `runtimeConfig.public` are sent to the client. Move secrets to the top level of `runtimeConfig` for server-only access.
```

**EnvLeak + generic (no framework detected):**
```
**Remove the secret from client-side code.**

Environment variables containing secrets should never be included in browser-accessible JavaScript. Check your build configuration to ensure secrets are only available server-side.

Common patterns:
- **Next.js:** Remove `NEXT_PUBLIC_` prefix from secret vars
- **SvelteKit:** Remove `PUBLIC_` prefix and use `$env/static/private`
- **Nuxt:** Move from `runtimeConfig.public` to `runtimeConfig` root
```

**SupabaseRls + any framework:**
```
**In your Supabase Dashboard or via SQL:**
\```sql
-- Enable RLS on the affected table
ALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;

-- Create a policy for authenticated access only
CREATE POLICY "Users can only access own data"
ON {table_name}
FOR ALL
USING (auth.uid() = user_id);
\```

Row Level Security (RLS) prevents unauthenticated access to your database tables via the public API. Without RLS, anyone with your Supabase URL and anon key can read all data.
```

**FirebaseRules + any framework:**
```
**In your Firebase Console > Realtime Database > Rules:**
\```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "public": {
      ".read": true,
      ".write": false
    }
  }
}
\```

Replace your current permissive rules (`".read": true, ".write": true`) with authenticated-only access. Create specific paths for genuinely public data.
```

**UnprotectedRoute + nextjs:**
```
**Create `middleware.ts` in your project root:**
\```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  if (request.nextUrl.pathname.startsWith('/api/')) {
    const token = request.cookies.get('auth-token');
    if (!token) {
      return NextResponse.json(
        { error: 'Unauthorized' },
        { status: 401 }
      );
    }
  }
  return NextResponse.next();
}

export const config = {
  matcher: '/api/:path*'
};
\```

This middleware checks for authentication before any API route executes. Without it, your API routes are accessible to anyone who knows the URL.
```

**UnprotectedRoute + sveltekit:**
```
**In your `src/hooks.server.ts`:**
\```typescript
import type { Handle } from '@sveltejs/kit';

export const handle: Handle = async ({ event, resolve }) => {
  if (event.url.pathname.startsWith('/api/')) {
    const session = event.cookies.get('session');
    if (!session) {
      return new Response(
        JSON.stringify({ error: 'Unauthorized' }),
        { status: 401, headers: { 'Content-Type': 'application/json' } }
      );
    }
  }
  return resolve(event);
};
\```

SvelteKit's server hooks run before every request. Use them to enforce authentication on API routes.
```

**UnprotectedRoute + generic:**
```
**Add authentication middleware to your API routes.**

Ensure all sensitive API endpoints check for a valid session or token before processing requests. Unauthenticated API routes are a common vulnerability in AI-generated code because code generators often skip auth middleware.
```

**NetlifyExposure:**
```
**In your `netlify.toml`:**
\```toml
[[redirects]]
  from = "/.netlify/*"
  to = "/404"
  status = 404
  force = true
\```

This prevents direct access to internal Netlify function endpoints and configuration paths. Functions should only be accessed through your application's defined routes.
```

**VercelEnvLeak:**
```
**Review your environment variable configuration in Vercel Dashboard.**

Deployment metadata variables (VERCEL_ENV, VERCEL_URL) are low-risk info disclosure, but avoid exposing them in client bundles. Use server-side only access or remove NEXT_PUBLIC_ prefix if present.
```

3. Extract variable names from raw_evidence when available (e.g., if evidence contains "NEXT_PUBLIC_STRIPE_SECRET_KEY", use "STRIPE_SECRET_KEY" in the remediation diff). Fall back to placeholder {VAR_NAME} if extraction fails.

4. Per user decision: NO "verify your fix" sections. End each remediation with the explanation sentence.

5. Add unit tests:
   - Test generate_remediation("supabase-rls", ..., Some("nextjs"), ...) returns SQL with RLS
   - Test generate_remediation("nextjs-env-leak", ..., Some("nextjs"), ...) returns diff removing NEXT_PUBLIC_
   - Test generate_remediation("nextjs-env-leak", ..., None, ...) returns generic multi-framework guidance
   - Test generate_remediation("unprotected-api-routes", ..., Some("sveltekit"), ...) returns hooks.server.ts code
   - Test all returned strings contain a code block (``` marker)
   - Test all returned strings do NOT contain "verify" or "Verify" (per user decision)
  </action>
  <verify>
    Run `cargo test` to verify remediation unit tests pass.
    Run `cargo check` to verify module compiles.
    Grep for "verify" in remediation output strings to confirm none exist.
  </verify>
  <done>
    Remediation engine generates framework-specific copy-paste code fixes for all vibe-code vulnerability types.
    Each fix includes targeted diff + 1-2 sentence explanation.
    No "verify your fix" sections (users rescan to verify per decision).
    Handles all 4 frameworks + generic fallback.
    Extracts variable names from evidence when available.
    Unit tests pass.
  </done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo test` passes including all remediation tests
- `grep -c "```" src/scanners/remediation.rs` shows code blocks in remediation strings
- Remediation covers: EnvLeak, SupabaseRls, FirebaseRules, UnprotectedRoute, NetlifyExposure, VercelEnvLeak
</verification>

<success_criteria>
1. Remediation engine generates targeted diffs for each (vuln_type, framework) combination
2. Each remediation includes code block + 1-2 sentence explanation
3. No "verify your fix" sections in any output
4. Generic fallback when framework not detected
5. Variable name extraction from evidence
6. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-vibe-code-intelligence/03-03-SUMMARY.md`
</output>
