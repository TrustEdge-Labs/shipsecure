---
phase: 05-codebase-preparation
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - README.md
  - .planning/research/STACK.md
  - .planning/research/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "README accurately describes the current deployment setup (Docker Compose, not Render)"
    - "README Getting Started instructions use docker compose up workflow"
    - "README Configuration table matches .env.example (complete list of variables)"
    - "Render is only mentioned as a platform TrustEdge can scan, not as TrustEdge's own hosting"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation reflecting DigitalOcean deployment"
      contains: "docker compose up"
    - path: ".planning/research/STACK.md"
      provides: "Updated stack research reflecting DigitalOcean deployment"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Configuration section references .env.example"
      pattern: "\\.env\\.example"
---

<objective>
Update README.md and planning documentation to reflect the current deployment setup (Docker Compose on DigitalOcean), remove stale Render hosting references, and ensure Getting Started instructions match the new configuration structure.

Purpose: Ensures documentation is accurate for anyone cloning the repo. Satisfies CLEAN-01 requirement (remove Render deployment references).
Output: Updated README.md with correct setup instructions and configuration table.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codebase-preparation/05-CONTEXT.md
@.planning/phases/05-codebase-preparation/05-01-SUMMARY.md
@.planning/phases/05-codebase-preparation/05-02-SUMMARY.md
@README.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md</name>
  <files>README.md</files>
  <action>
Update README.md with the following changes:

1. **"What It Scans" section -- Platform detection line:**
   Keep "Render" in the platform detection list -- TrustEdge scans apps HOSTED on Render, so this is a legitimate feature. But ensure the description is clear: "Auto-detects Vercel, Netlify, Railway, Render, Supabase, Firebase" is about apps being scanned, not TrustEdge itself.

   In the "Paid audit" bullet for vibe-code checks, keep "Railway/Render debug endpoints" -- same reason, these are scan targets.

2. **Tech Stack table:**
   Change "Scanners" description from "Nuclei (containerized), testssl.sh, custom probes" to "Nuclei (native binary), testssl.sh (native binary), custom probes".
   This reflects that scanners now run as native binaries, not Docker containers.

3. **Getting Started > Prerequisites:**
   Update to:
   - Docker and Docker Compose (for full-stack development)
   - OR for native development: Rust 1.88+, Node.js 20+, PostgreSQL 16, Nuclei, testssl.sh

4. **Getting Started > Quick Start with Docker:**
   Update to:
   ```bash
   cp .env.example .env
   # Edit .env with your configuration (all required variables must be set)
   docker compose up
   ```
   Note: Use `docker compose` (v2 plugin) instead of `docker-compose` (legacy standalone).

5. **Getting Started > Local Development:**
   Update to reflect that scanners run natively:
   ```bash
   # Start just the database
   docker compose up db

   # Backend (separate terminal)
   cp .env.example .env
   # Edit .env -- see .env.example for all required variables
   cargo build
   cargo run  # Runs on http://localhost:3000

   # Frontend (separate terminal)
   cd frontend
   npm install
   npm run dev  # Runs on http://localhost:3001
   ```
   Remove `sqlx migrate run` line -- migrations run automatically on startup.

6. **Configuration table:**
   Replace the current 7-row table with a complete table matching .env.example. Include ALL variables from the .env.example created in Plan 02. Mark required vs optional clearly.

   | Variable | Description | Required |
   |----------|-------------|----------|
   | DATABASE_URL | PostgreSQL connection string | Yes |
   | PORT | Backend HTTP port | Yes |
   | RUST_LOG | Log level filter | Yes |
   | TRUSTEDGE_BASE_URL | Base URL for email links | Yes |
   | FRONTEND_URL | Frontend URL for redirects | Yes |
   | MAX_CONCURRENT_SCANS | Maximum parallel scans | Yes |
   | RESEND_API_KEY | Resend API key for email delivery | No (email disabled) |
   | STRIPE_SECRET_KEY | Stripe secret key | No (checkout disabled) |
   | STRIPE_WEBHOOK_SECRET | Stripe webhook validation secret | No (webhooks disabled) |
   | NUCLEI_BINARY_PATH | Path to Nuclei binary | No (auto-detected) |
   | TESTSSL_BINARY_PATH | Path to testssl.sh binary | No (auto-detected) |
   | TRUSTEDGE_TEMPLATES_DIR | Custom Nuclei templates directory | No (uses bundled) |

7. **Architecture Highlights:**
   Change "Container hardening — CIS-hardened Docker execution..." to "Scanner execution — Nuclei and testssl.sh run as native binaries with configurable paths and graceful degradation".
   Keep the rest unchanged.

8. **Remove mention of Docker requirement for scanners.** The prerequisites section should not say "Docker (for containerized scanners)" -- Docker is for the full-stack dev environment only, not for scanner execution.
  </action>
  <verify>
Read README.md and verify: (1) no "Render" references as TrustEdge hosting provider, (2) Render only mentioned as scan target platform, (3) Configuration table lists all 12 env vars, (4) Getting Started uses `docker compose` (v2), (5) Tech stack says "native binary" not "containerized".
  </verify>
  <done>
README accurately describes the current deployment setup. Getting Started instructions work for both Docker Compose and native development. Configuration table is complete. Render is only mentioned as a platform TrustEdge scans, not as its own hosting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up Render hosting references in planning docs</name>
  <files>.planning/research/STACK.md, .planning/research/SUMMARY.md</files>
  <action>
These are planning/research documents that reference "Render" as TrustEdge's hosting platform (historical -- v1.0 was originally planned for Render). The references are in historical context, so they should be updated to reflect the migration but not deleted (they're research notes).

1. **`.planning/research/STACK.md`:**
   - At the top, add a note: `**Note:** This research was conducted for the original v1.0 Render deployment. As of v1.1, TrustEdge deploys to DigitalOcean. See .planning/phases/05-codebase-preparation/ for migration context.`
   - Do NOT rewrite the entire document -- just add the context note.

2. **`.planning/research/SUMMARY.md`:**
   - At the top, add the same migration context note.
   - Do NOT rewrite -- historical research context is valuable.

These files are `.planning/` research archives. The goal is to prevent future confusion, not rewrite history.

**Do NOT modify:**
- `.planning/phases/01-foundation/*` (historical phase context)
- `.planning/phases/02-free-tier-mvp/*` (historical phase context)
- `.planning/phases/05-codebase-preparation/05-CONTEXT.md` (current phase, locked)
- `.planning/phases/05-codebase-preparation/05-RESEARCH.md` (current phase, locked)
- `.planning/ROADMAP.md` (references Render cleanup as a requirement, which is correct)
- `.planning/PROJECT.md` (references migration decision, which is correct)
- `.planning/STATE.md` (references migration decision, which is correct)
  </action>
  <verify>
Check that `.planning/research/STACK.md` and `.planning/research/SUMMARY.md` have the migration context note at the top. Verify no other planning files were modified.
  </verify>
  <done>
Planning research docs have migration context notes. Historical Render references preserved for context but clearly marked as superseded by DigitalOcean deployment.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Render" README.md` -- should only match platform detection lines (scanning Render-hosted apps), not TrustEdge hosting references
2. README Configuration table has 12 rows (all env vars from .env.example)
3. README Getting Started uses `docker compose` (v2 syntax)
4. Planning research docs have migration notes
</verification>

<success_criteria>
README accurately describes the current DigitalOcean deployment setup with complete configuration documentation. Render is only referenced as a scan target platform. Planning docs have migration context notes on historical Render references.
</success_criteria>

<output>
After completion, create `.planning/phases/05-codebase-preparation/05-04-SUMMARY.md`
</output>
