---
phase: 30-stripe-removal-and-schema-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/api/checkout.rs
  - src/api/webhooks.rs
  - src/api/mod.rs
  - src/main.rs
  - src/lib.rs
  - src/pdf.rs
  - src/models/mod.rs
  - src/models/paid_audit.rs
  - src/db/mod.rs
  - src/db/paid_audits.rs
  - src/orchestrator/worker_pool.rs
  - src/email/mod.rs
  - migrations/20260218000001_stripe_removal_schema.sql
  - frontend/components/upgrade-cta.tsx
  - frontend/app/payment/success/page.tsx
  - frontend/app/payment/success/layout.tsx
  - frontend/app/results/[token]/page.tsx
  - frontend/e2e/paid-audit.spec.ts
  - frontend/e2e/fixtures/checkout.ts
  - frontend/e2e/free-scan.spec.ts
  - frontend/e2e/helpers/route-mocks.ts
  - frontend/__tests__/components/UpgradeCTA.test.tsx
  - frontend/__tests__/components/dark-mode.test.tsx
  - frontend/__tests__/helpers/fixtures/checkout.ts
  - frontend/__tests__/helpers/msw/handlers.ts
autonomous: true
requirements: [CLEN-01, CLEN-02, CLEN-03]

must_haves:
  truths:
    - "cargo check succeeds with async-stripe, hmac, sha2, genpdf, and hex removed from Cargo.toml"
    - "No Stripe routes exist — /api/v1/checkout and /api/v1/webhooks/stripe are gone from main.rs"
    - "Deleting a scan row sets paid_audits.scan_id to NULL instead of cascade-deleting the paid_audit row"
    - "INSERT INTO scans with tier='authenticated' and a clerk_user_id value succeeds without constraint violations"
    - "Frontend builds successfully — npm run build produces no errors"
    - "E2E free scan test passes without referencing UpgradeCTA or Upgrade to Deep Audit"
  artifacts:
    - path: "migrations/20260218000001_stripe_removal_schema.sql"
      provides: "FK change to SET NULL, stripe_events DROP, tier constraint extension, clerk_user_id column"
      contains: "ON DELETE SET NULL"
    - path: "Cargo.toml"
      provides: "Cleaned dependency list without Stripe crates"
    - path: "src/api/webhooks.rs"
      provides: "Clerk webhook handler only — no Stripe code"
      contains: "handle_clerk_webhook"
  key_links:
    - from: "src/main.rs"
      to: "src/api/webhooks.rs"
      via: "only clerk webhook route remains"
      pattern: "webhooks/clerk"
    - from: "src/orchestrator/worker_pool.rs"
      to: "scan tier matching"
      via: "no paid tier branch — free config for all non-free tiers"
      pattern: "_ =>"
    - from: "migrations/20260218000001_stripe_removal_schema.sql"
      to: "paid_audits table"
      via: "FK constraint change"
      pattern: "ON DELETE SET NULL"
---

<objective>
Remove all Stripe dependencies from the Rust backend and Next.js frontend, run a database migration to change the paid_audits FK to ON DELETE SET NULL, drop the stripe_events table, extend the scans tier constraint to include 'authenticated', and add a clerk_user_id column to scans.

Purpose: Eliminate dead Stripe code and prepare the schema for authenticated scan ownership before results gating (Phase 31) and tiered access (Phase 33) can be built.
Output: Clean-compiling backend with no Stripe crates, building frontend with no UpgradeCTA, migration file for FK/tier/clerk_user_id changes.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-stripe-removal-and-schema-cleanup/30-RESEARCH.md
@.planning/phases/29-auth-foundation/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Stripe backend code, crates, and create schema migration</name>
  <files>
    Cargo.toml
    src/api/checkout.rs
    src/api/webhooks.rs
    src/api/mod.rs
    src/main.rs
    src/lib.rs
    src/pdf.rs
    src/models/mod.rs
    src/models/paid_audit.rs
    src/db/mod.rs
    src/db/paid_audits.rs
    src/orchestrator/worker_pool.rs
    src/email/mod.rs
    migrations/20260218000001_stripe_removal_schema.sql
  </files>
  <action>
    **Order matters: remove Rust code FIRST, then crates from Cargo.toml (compiler errors are clearer this way).**

    **Step 1 — Delete entire files:**
    - Delete `src/api/checkout.rs` (Stripe checkout handler)
    - Delete `src/pdf.rs` (genpdf PDF generation, only called from paid audit webhook)
    - Delete `src/models/paid_audit.rs` (PaidAudit, PaidAuditStatus, StripeEvent structs)
    - Delete `src/db/paid_audits.rs` (all paid_audit DB functions)

    **Step 2 — Edit backend files (partial removal):**

    `src/api/webhooks.rs`:
    - Remove `handle_stripe_webhook` function and `handle_checkout_completed` function
    - Remove `use hmac::{Hmac, Mac};`, `use sha2::Sha256;`, `type HmacSha256 = Hmac<Sha256>;`, and `use hex;` (or similar hex import)
    - KEEP `handle_clerk_webhook` and all its imports (`svix::webhooks::Webhook`, `axum::body::Bytes`, etc.)
    - KEEP `use uuid::Uuid;` only if it is used by the remaining clerk webhook code — otherwise remove it too

    `src/api/mod.rs`:
    - Remove `pub mod checkout;`
    - KEEP all other module declarations

    `src/main.rs`:
    - Remove `checkout` from the `use shipsecure::api::{...}` import
    - Remove `.route("/api/v1/checkout", post(checkout::create_checkout))`
    - Remove `.route("/api/v1/webhooks/stripe", post(webhooks::handle_stripe_webhook))`
    - KEEP the clerk webhook route `.route("/api/v1/webhooks/clerk", ...)`
    - KEEP all other routes and imports

    `src/lib.rs`:
    - Remove `pub mod pdf;`
    - KEEP all other module declarations

    `src/models/mod.rs`:
    - Remove `pub mod paid_audit;` and `pub use paid_audit::{PaidAudit, PaidAuditStatus, StripeEvent};`
    - KEEP all other module declarations and re-exports

    `src/db/mod.rs`:
    - Remove `pub mod paid_audits;`
    - KEEP `pub mod scans;` and `pub mod findings;`

    `src/orchestrator/worker_pool.rs`:
    - Delete the `spawn_paid_scan` method entirely
    - In the `run_scanners` tier match: remove the `"paid"` arm. Change to use `_ =>` defaulting to free-tier config (so any non-"free" tier value gets free config). The `"free"` arm stays as-is.
    - Remove the `crate::db::paid_audits::clear_findings_by_scan` call if present
    - Do NOT remove `use base64::Engine;` — it is still needed for results token generation (line ~355)

    `src/email/mod.rs`:
    - Remove `send_paid_audit_email` function entirely
    - KEEP `send_scan_complete_email` and all other functions
    - Do NOT remove `base64` usage if any remains — but check if the email module itself uses base64 (only `send_paid_audit_email` does, so the import in this file can go if it becomes unused)

    **Step 3 — Remove crates from Cargo.toml:**
    Remove these 5 dependency lines entirely:
    - `async-stripe = { ... }`
    - `genpdf = "0.2"`
    - `hex = "0.4"`
    - `hmac = "0.12"`
    - `sha2 = "0.10"`

    Do NOT remove:
    - `base64` — used in `worker_pool.rs` for results token generation
    - `svix` — used in `handle_clerk_webhook` for webhook signature verification

    **Step 4 — Create migration file:**
    Create `migrations/20260218000001_stripe_removal_schema.sql` with this exact content:

    ```sql
    -- Phase 30: Stripe Removal and Schema Cleanup
    -- CLEN-02: Change paid_audits FK from CASCADE to SET NULL
    -- CLEN-03: Extend tier constraint and add clerk_user_id

    -- 1. Change paid_audits FK from CASCADE to SET NULL
    --    Must drop NOT NULL first since SET NULL requires a nullable column
    ALTER TABLE paid_audits ALTER COLUMN scan_id DROP NOT NULL;
    ALTER TABLE paid_audits DROP CONSTRAINT IF EXISTS paid_audits_scan_id_fkey;
    ALTER TABLE paid_audits ADD CONSTRAINT paid_audits_scan_id_fkey
      FOREIGN KEY (scan_id) REFERENCES scans(id) ON DELETE SET NULL;

    -- 2. Drop the Stripe events idempotency table (no longer needed)
    DROP TABLE IF EXISTS stripe_events;

    -- 3. Extend tier CHECK constraint to include 'authenticated'
    ALTER TABLE scans DROP CONSTRAINT IF EXISTS scans_tier_check;
    ALTER TABLE scans ADD CONSTRAINT scans_tier_check
      CHECK (tier IN ('free', 'paid', 'authenticated'));

    -- 4. Add clerk_user_id to scans (nullable FK to users table)
    ALTER TABLE scans ADD COLUMN IF NOT EXISTS clerk_user_id TEXT
      REFERENCES users(clerk_user_id) ON DELETE SET NULL;

    CREATE INDEX IF NOT EXISTS idx_scans_clerk_user_id ON scans(clerk_user_id);
    ```

    **Step 5 — Verify compilation:**
    Run `cargo check` from the project root. It must pass with zero errors. Warnings about unused imports are acceptable only if they are pre-existing (not introduced by this change). Fix any unused import warnings caused by this cleanup.
  </action>
  <verify>
    1. `cargo check` passes with no errors
    2. `grep -r "async.stripe\|stripe::" src/` returns no matches
    3. `grep -r "genpdf\|hmac::\|sha2::" src/` returns no matches
    4. `grep -r "checkout" src/api/mod.rs src/main.rs` returns no matches
    5. `grep "pub mod pdf" src/lib.rs` returns no matches
    6. `ls src/api/checkout.rs src/pdf.rs src/models/paid_audit.rs src/db/paid_audits.rs 2>&amp;1` shows "No such file or directory" for all four
    7. Migration file `migrations/20260218000001_stripe_removal_schema.sql` exists and contains "ON DELETE SET NULL" and "authenticated"
  </verify>
  <done>
    Backend compiles cleanly without async-stripe, hmac, sha2, genpdf, and hex. No Stripe route handlers exist. Migration file is ready to apply. The handle_clerk_webhook function and all non-Stripe backend code is untouched and working.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Stripe frontend components and fix affected tests</name>
  <files>
    frontend/components/upgrade-cta.tsx
    frontend/app/payment/success/page.tsx
    frontend/app/payment/success/layout.tsx
    frontend/app/results/[token]/page.tsx
    frontend/e2e/paid-audit.spec.ts
    frontend/e2e/fixtures/checkout.ts
    frontend/e2e/free-scan.spec.ts
    frontend/e2e/helpers/route-mocks.ts
    frontend/__tests__/components/UpgradeCTA.test.tsx
    frontend/__tests__/components/dark-mode.test.tsx
    frontend/__tests__/helpers/fixtures/checkout.ts
    frontend/__tests__/helpers/msw/handlers.ts
  </files>
  <action>
    **Step 1 — Delete entire files:**
    - Delete `frontend/components/upgrade-cta.tsx`
    - Delete `frontend/app/payment/success/page.tsx`
    - Delete `frontend/app/payment/success/layout.tsx`
    - Delete `frontend/e2e/paid-audit.spec.ts`
    - Delete `frontend/e2e/fixtures/checkout.ts`
    - Delete `frontend/__tests__/components/UpgradeCTA.test.tsx`
    - Delete `frontend/__tests__/helpers/fixtures/checkout.ts`

    Also delete the `frontend/app/payment/` directory entirely if it only contains the `success/` subdirectory.

    **Step 2 — Edit frontend files (partial removal):**

    `frontend/app/results/[token]/page.tsx`:
    - Remove `import { UpgradeCTA } from '@/components/upgrade-cta'`
    - Remove the JSX block: `{data.tier === 'free' && (<div className="mb-6"><UpgradeCTA ... /></div>)}`
    - KEEP everything else unchanged

    `frontend/__tests__/helpers/msw/handlers.ts`:
    - Remove `import { checkoutFixtures }` (or however checkout fixtures are imported)
    - Remove the checkout handler (`http.post('*/api/v1/checkout', ...)`)
    - Remove the stripe webhook handler (`http.post('*/api/v1/webhooks/stripe', ...)`)
    - Remove `checkoutServerError` from the `errorHandlers` array (or wherever it is defined)
    - KEEP all other handlers intact (scan, results, clerk webhook, etc.)

    `frontend/__tests__/components/dark-mode.test.tsx`:
    - Remove `import { UpgradeCTA } from '@/components/upgrade-cta'` (or similar import)
    - Remove the two test cases that render/test UpgradeCTA (look for test blocks containing "UpgradeCTA")
    - KEEP all other test cases unchanged

    `frontend/e2e/helpers/route-mocks.ts`:
    - Remove the `mockCheckout` function and its Stripe redirect mock
    - KEEP `mockScanPolling`, `mockNetworkFailure`, and all other helper functions

    `frontend/e2e/free-scan.spec.ts`:
    - Remove the assertion `await expect(page.locator('text=Upgrade to Deep Audit')).toBeVisible();` (or similar UpgradeCTA visibility check)
    - KEEP all other assertions in the free scan test

    **Step 3 — Verify build and tests:**
    - Run `cd frontend && npm run build` — must succeed with no errors
    - Run `cd frontend && npx vitest run` — unit tests must pass (dark-mode tests will have fewer cases, UpgradeCTA tests gone)
    - Do NOT run E2E tests (they require a running backend) — but confirm `npx playwright test --list` shows no paid-audit specs
  </action>
  <verify>
    1. `npm run build` in frontend/ succeeds with no errors
    2. `npx vitest run` in frontend/ passes all remaining tests
    3. `ls frontend/components/upgrade-cta.tsx 2>&amp;1` shows "No such file or directory"
    4. `ls frontend/app/payment/ 2>&amp;1` shows "No such file or directory"
    5. `grep -r "UpgradeCTA\|upgrade-cta" frontend/app/ frontend/components/ frontend/__tests__/ frontend/e2e/` returns no matches
    6. `grep -r "checkout\|checkoutFixtures\|paid-audit" frontend/__tests__/helpers/msw/handlers.ts frontend/e2e/helpers/route-mocks.ts` returns no matches
    7. `npx playwright test --list 2>&amp;1 | grep -i paid` returns no matches
  </verify>
  <done>
    Frontend builds cleanly with no Stripe/UpgradeCTA references. All unit tests pass. No paid-audit E2E specs exist. The free-scan E2E test no longer asserts on the deleted UpgradeCTA component. The results page renders without the upgrade CTA block.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase success criteria:

1. **CLEN-01 (Stripe removal):** `cargo check` passes AND `npm run build` passes — no compilation errors from removed dependencies
2. **CLEN-02 (FK change):** Migration file contains `ON DELETE SET NULL` for paid_audits — verify with `grep "ON DELETE SET NULL" migrations/20260218000001_stripe_removal_schema.sql`
3. **CLEN-03 (clerk_user_id + tier):** Migration file contains `clerk_user_id TEXT` and `'authenticated'` in tier CHECK — verify with `grep "clerk_user_id\|authenticated" migrations/20260218000001_stripe_removal_schema.sql`
4. **No regressions:** `cargo test` passes for backend, `npx vitest run` passes for frontend unit tests
5. **Clean codebase:** No orphaned Stripe imports, no dead code warnings introduced by this phase
</verification>

<success_criteria>
- Backend compiles without async-stripe, hmac, sha2, genpdf, hex — `cargo check` clean
- Frontend builds without UpgradeCTA — `npm run build` clean
- Migration file ready: paid_audits FK ON DELETE SET NULL, stripe_events dropped, tier constraint extended, clerk_user_id added
- All existing tests pass (backend cargo test, frontend vitest)
- No Stripe-related code remains in src/ or frontend/
</success_criteria>

<output>
After completion, create `.planning/phases/30-stripe-removal-and-schema-cleanup/30-01-SUMMARY.md`
</output>
