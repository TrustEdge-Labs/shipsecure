---
phase: 11-mobile-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/page.tsx
  - frontend/app/privacy/page.tsx
  - frontend/app/terms/page.tsx
  - frontend/app/scan/[id]/page.tsx
  - frontend/app/results/[token]/page.tsx
  - frontend/app/payment/success/page.tsx
  - frontend/components/grade-summary.tsx
  - frontend/components/finding-accordion.tsx
  - frontend/components/results-dashboard.tsx
  - frontend/components/upgrade-cta.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page renders without horizontal scroll at 375px viewport width"
    - "Results page grade summary and findings render without overflow or overlapping at 375px"
    - "All pages use the global Footer component (no duplicate inline footers)"
    - "Buttons have consistent sizing, colors, and hover states across all pages"
    - "Touch targets are at least 44px on interactive elements"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Landing page without duplicate footer, responsive at all breakpoints"
      contains: "ScanForm"
    - path: "frontend/app/privacy/page.tsx"
      provides: "Privacy page without duplicate footer, using flex-1 layout"
    - path: "frontend/app/terms/page.tsx"
      provides: "Terms page without duplicate footer, using flex-1 layout"
    - path: "frontend/components/grade-summary.tsx"
      provides: "Grade summary that stacks vertically on mobile"
      contains: "flex-col"
    - path: "frontend/components/finding-accordion.tsx"
      provides: "Finding accordion with mobile-friendly touch targets and text wrapping"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/footer.tsx"
      via: "Root layout renders Footer globally"
      pattern: "Footer"
    - from: "frontend/components/grade-summary.tsx"
      to: "frontend/app/results/[token]/page.tsx"
      via: "GradeSummary imported in results page"
      pattern: "GradeSummary"
---

<objective>
Make all pages mobile-responsive and visually consistent.

Purpose: UX-01 (mobile 375px), UX-02 (tablet 768px), and UX-05 (visual consistency) require that every page renders correctly without horizontal scroll, overlapping elements, or duplicate footers. The global Footer component (from Phase 10) is already in root layout.tsx but several pages render their own inline footers, causing duplication.

Output: All 6 pages render cleanly at 375px, 768px, and 1024px+ with consistent styling and no duplicate footers.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ux-polish/11-RESEARCH.md

@frontend/app/layout.tsx
@frontend/app/page.tsx
@frontend/app/privacy/page.tsx
@frontend/app/terms/page.tsx
@frontend/app/scan/[id]/page.tsx
@frontend/app/results/[token]/page.tsx
@frontend/app/payment/success/page.tsx
@frontend/components/grade-summary.tsx
@frontend/components/finding-accordion.tsx
@frontend/components/results-dashboard.tsx
@frontend/components/upgrade-cta.tsx
@frontend/components/footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix duplicate footers and page layout consistency</name>
  <files>
    frontend/app/page.tsx
    frontend/app/privacy/page.tsx
    frontend/app/terms/page.tsx
  </files>
  <action>
    The root layout.tsx already renders the global Footer component inside a flex column layout with `min-h-screen`. Three pages currently render their own inline footer elements, creating duplicate footers. Fix all three:

    **frontend/app/page.tsx (Landing page):**
    - Remove the entire `<footer>` element at the bottom of the page (lines ~178-183). The global Footer component in layout.tsx handles this.
    - Remove the outer `<div className="min-h-screen ...">` wrapper. The root layout already provides `min-h-screen` via the flex column. Replace with a fragment or just keep the `<main>` as the top-level element. The page content should be wrapped in a simple container, not set its own min-height (which conflicts with the flex layout that pins the global footer).
    - Keep all existing responsive classes (the landing page already has good `sm:` and `md:` breakpoints).

    **frontend/app/privacy/page.tsx:**
    - Remove the entire `<footer>` element at the bottom (lines ~301-306).
    - Remove the outer `<div className="min-h-screen ...">` wrapper. Replace with a fragment or just `<main>` as top-level element. The root layout flex column handles footer pinning.
    - The `py-16` top padding is fine but should also have `pb-8` or similar bottom padding to work with the global footer.

    **frontend/app/terms/page.tsx:**
    - Same treatment as privacy: remove inline `<footer>` element, remove `min-h-screen` wrapper div.
    - The terms page has the same structural pattern as privacy -- apply identical changes.

    IMPORTANT: Do NOT touch the `<Footer />` component in layout.tsx or the footer.tsx component file. Those are correct.
  </action>
  <verify>
    Run `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build` to confirm no build errors. Then grep for duplicate footer patterns: `grep -r "<footer" frontend/app/` should only find zero results (all footers should be gone from page files since the global one is in layout.tsx).
  </verify>
  <done>
    Landing, Privacy, and Terms pages no longer render duplicate footers. All pages use the global Footer from root layout.tsx. No `min-h-screen` conflicts with the root layout's flex column.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile-responsive fixes for results page and components</name>
  <files>
    frontend/app/results/[token]/page.tsx
    frontend/app/scan/[id]/page.tsx
    frontend/app/payment/success/page.tsx
    frontend/components/grade-summary.tsx
    frontend/components/finding-accordion.tsx
    frontend/components/results-dashboard.tsx
    frontend/components/upgrade-cta.tsx
  </files>
  <action>
    Fix mobile layout issues in the results ecosystem and scan progress page:

    **frontend/components/grade-summary.tsx:**
    The current layout uses `flex items-center gap-6` which overflows on mobile because grade circle + framework text + severity badges all try to fit in one row.
    - Change the outer container from `flex items-center gap-6` to `flex flex-col sm:flex-row sm:items-center gap-4 sm:gap-6`.
    - On mobile, the grade circle should be at the top-left, framework/platform text below it, and severity badges wrapping below that.
    - The grade circle (w-12 h-12) is fine at all sizes.
    - The finding counts `flex flex-wrap gap-3` is already good, but add `mt-2 sm:mt-0` to separate it from the text above on mobile.
    - Change `flex-1` on the finding counts div to just nothing or `w-full sm:flex-1` so it takes full width on mobile.

    **frontend/components/finding-accordion.tsx:**
    - The accordion header button uses `flex items-center gap-3` which can overflow on mobile when severity badge + vibe-code badge + title + scanner name + chevron all compete for space.
    - Change to: `flex flex-wrap items-center gap-2 sm:gap-3`. The title `<span>` should have `min-w-0` and the text should have `break-words` to prevent long finding titles from causing overflow.
    - The scanner name span (text-xs) should be hidden on very small screens or moved to the expanded body. Use `hidden sm:inline` on the scanner name span so mobile shows just severity + title + chevron.
    - Ensure the expanded body text has `break-words` class to handle long URLs or code snippets in descriptions/remediation.

    **frontend/components/results-dashboard.tsx:**
    - The toggle buttons ("By Severity" / "By Category") are fine but ensure they have `min-h-[44px]` for touch targets.

    **frontend/components/upgrade-cta.tsx:**
    - The "Upgrade for $49" button and "One-time payment" text use `flex items-center gap-4` which works, but ensure the button has `min-h-[44px]` for touch targets.
    - The feature list items `flex items-start gap-2` are fine on mobile already.

    **frontend/app/results/[token]/page.tsx:**
    - The results page outer container uses `py-8 px-4` which is fine but the target URL can overflow on mobile. The `font-mono` link for the target URL already has no special wrapping. Add `break-all` class to the target URL anchor element (it already exists on the scan progress page but not on results).
    - The actions section `flex gap-4 flex-wrap` is already responsive. Ensure buttons have `min-h-[44px]` for touch targets. Add `w-full sm:w-auto` to both action links so they stack full-width on mobile.
    - Change the header section `space-y-2 text-sm` -- the expiry badge with `inline-block` should become `block sm:inline-block` to prevent overflow on mobile.

    **frontend/app/scan/[id]/page.tsx:**
    - Already uses `max-w-md w-full` centered layout which works well on mobile.
    - Ensure the "Start New Scan" and "Try Again" links have `min-h-[44px] flex items-center justify-center` for consistent touch targets.
    - The error message box with `font-mono bg-gray-100` should have `break-all` to prevent long error messages from overflowing.

    **frontend/app/payment/success/page.tsx:**
    - Already uses centered `max-w-md w-full` layout. This is fine.
    - Ensure the "Return to Home" link has `min-h-[44px]` for touch target.
  </action>
  <verify>
    Run `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build` to confirm no build errors. Visually verify by checking that no component uses fixed-width classes wider than 375px or has flex layouts without wrapping.
  </verify>
  <done>
    GradeSummary stacks vertically on mobile. FindingAccordion wraps content without overflow. All interactive elements have minimum 44px touch targets. Long URLs and error messages wrap correctly. Action buttons stack full-width on mobile.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/john/projects/github.com/trustedge-audit/frontend && npx next build` succeeds with no errors
2. `grep -r "<footer" frontend/app/` returns no results (no duplicate footers in page files)
3. `grep -r "min-h-screen" frontend/app/` should only appear in scan/[id]/page.tsx (which uses it for centering, which is fine since it's inside the flex-1 div from layout)
4. All components with flex layouts have `flex-wrap` or `flex-col` breakpoints for mobile
</verification>

<success_criteria>
- All 6 pages render without horizontal scroll at 375px viewport width
- No duplicate footers on any page
- GradeSummary stacks vertically on mobile viewports
- Finding titles and URLs wrap without overflow
- All buttons and links have at least 44px touch target height
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ux-polish/11-01-SUMMARY.md`
</output>
