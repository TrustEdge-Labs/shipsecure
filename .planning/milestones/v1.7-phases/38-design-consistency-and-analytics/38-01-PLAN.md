---
phase: 38-design-consistency-and-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/layout.tsx
autonomous: true
requirements:
  - ANLYT-01

must_haves:
  truths:
    - "Plausible analytics script tag contains data-domain=\"shipsecure.ai\" so the dashboard attributes traffic to the correct site"
  artifacts:
    - path: "frontend/app/layout.tsx"
      provides: "Root layout with corrected Plausible script tag"
      contains: "data-domain=\"shipsecure.ai\""
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "plausible.io analytics dashboard"
      via: "data-domain attribute on Script src tag"
      pattern: "data-domain"
---

<objective>
Add the missing `data-domain="shipsecure.ai"` attribute to the Plausible analytics script tag so traffic is correctly attributed in the Plausible dashboard.

Purpose: Without `data-domain`, Plausible cannot associate pageviews with the shipsecure.ai site — analytics are either lost or misattributed. This is a one-line fix.
Output: Root layout with a correctly configured Plausible script tag.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-domain attribute to Plausible script tag</name>
  <files>frontend/app/layout.tsx</files>
  <action>
In `frontend/app/layout.tsx`, find the `<Script>` component that loads the Plausible JS bundle. It currently reads:

```tsx
<Script
  src="https://plausible.io/js/pa-tyZW93JgybTFzRD4-tmty.js"
  strategy="afterInteractive"
/>
```

Add the `data-domain` attribute so Plausible knows which site to attribute traffic to:

```tsx
<Script
  src="https://plausible.io/js/pa-tyZW93JgybTFzRD4-tmty.js"
  strategy="afterInteractive"
  data-domain="shipsecure.ai"
/>
```

Do not modify any other part of the layout. The `plausible-init` script block and everything else stays exactly as-is.
  </action>
  <verify>
    <automated>grep -n 'data-domain' /home/john/vault/projects/github.com/shipsecure/frontend/app/layout.tsx</automated>
    <manual>Confirm output shows: data-domain="shipsecure.ai" on the Plausible Script line</manual>
  </verify>
  <done>layout.tsx Script tag for Plausible includes data-domain="shipsecure.ai" and no other changes were made</done>
</task>

</tasks>

<verification>
Run: `grep -n 'data-domain\|plausible' /home/john/vault/projects/github.com/shipsecure/frontend/app/layout.tsx`

Expected: Line with `data-domain="shipsecure.ai"` visible alongside the Plausible script src.
</verification>

<success_criteria>
- `data-domain="shipsecure.ai"` present on the Plausible `<Script>` tag in layout.tsx
- No other changes to layout.tsx
- File compiles without TypeScript errors: `cd /home/john/vault/projects/github.com/shipsecure/frontend && npx tsc --noEmit 2>&1 | head -20`
</success_criteria>

<output>
After completion, create `.planning/phases/38-design-consistency-and-analytics/38-01-SUMMARY.md`
</output>
