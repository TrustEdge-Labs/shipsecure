---
phase: 04-monetization
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/orchestrator/worker_pool.rs
  - src/scanners/vibecode.rs
  - src/scanners/exposed_files.rs
  - src/scanners/js_secrets.rs
  - templates/nuclei/paid/advanced-env-leak.yaml
  - templates/nuclei/paid/api-auth-bypass.yaml
  - templates/nuclei/paid/debug-endpoints.yaml
  - templates/nuclei/paid/database-exposure.yaml
  - templates/nuclei/paid/admin-panel-detection.yaml
autonomous: true

must_haves:
  truths:
    - "Orchestrator can execute scans with tier=paid using extended configuration"
    - "Paid tier runs more Nuclei templates than free tier"
    - "Paid tier scans more JS files (50 vs 10-20)"
    - "Paid tier probes more exposed file paths"
    - "Paid tier uses longer timeout (600s vs 180s)"
    - "Free tier scanning behavior is unchanged"
  artifacts:
    - path: "src/orchestrator/worker_pool.rs"
      provides: "Tier-aware scan execution with spawn_paid_scan method"
      exports: ["ScanOrchestrator"]
      min_lines: 100
    - path: "templates/nuclei/paid/advanced-env-leak.yaml"
      provides: "Extended Nuclei template for deeper env leak scanning"
    - path: "templates/nuclei/paid/api-auth-bypass.yaml"
      provides: "Nuclei template for API authentication bypass checks"
    - path: "templates/nuclei/paid/debug-endpoints.yaml"
      provides: "Nuclei template for debug/admin endpoint detection"
    - path: "templates/nuclei/paid/database-exposure.yaml"
      provides: "Nuclei template for database exposure detection"
    - path: "templates/nuclei/paid/admin-panel-detection.yaml"
      provides: "Nuclei template for admin panel detection"
  key_links:
    - from: "src/orchestrator/worker_pool.rs"
      to: "src/scanners/vibecode.rs"
      via: "tier parameter passed to scan_vibecode"
      pattern: "scan_vibecode.*tier"
    - from: "src/orchestrator/worker_pool.rs"
      to: "src/scanners/js_secrets.rs"
      via: "tier-based file limit parameter"
      pattern: "file_limit|max_files"
    - from: "src/orchestrator/worker_pool.rs"
      to: "src/scanners/exposed_files.rs"
      via: "tier-based path list parameter"
      pattern: "extended|paid|tier"
---

<objective>
Make the scan orchestrator tier-aware so paid audits run deeper, more comprehensive scans than free tier.

Purpose: This is the core value differentiation between free and paid tiers. Free tier runs passive reconnaissance (basic headers, TLS, limited file/secret scanning, basic Nuclei templates). Paid tier runs active probing with 5x more Nuclei templates, 50 JS files (vs ~20), extended exposed file paths, and a 600-second timeout. Users should consistently see 5-10x more findings in paid reports.

Output: Modified orchestrator with tier-aware execution, modified scanners accepting tier parameters, and 5 new paid-only Nuclei templates.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monetization/04-RESEARCH.md
@.planning/phases/04-monetization/04-01-SUMMARY.md
@src/orchestrator/worker_pool.rs
@src/scanners/vibecode.rs
@src/scanners/exposed_files.rs
@src/scanners/js_secrets.rs
@templates/nuclei/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create paid-tier Nuclei templates</name>
  <files>templates/nuclei/paid/advanced-env-leak.yaml, templates/nuclei/paid/api-auth-bypass.yaml, templates/nuclei/paid/debug-endpoints.yaml, templates/nuclei/paid/database-exposure.yaml, templates/nuclei/paid/admin-panel-detection.yaml</files>
  <action>
Create directory `templates/nuclei/paid/` and add 5 custom Nuclei templates for paid tier active probing.

These templates should probe for vulnerabilities that require active requests (not just passive header/response analysis). They go beyond the free tier's 7 existing templates.

1. `advanced-env-leak.yaml` — Check for environment variable leaks in non-obvious locations:
   - /.env.production, /.env.staging, /.env.local, /.env.development
   - /config.json, /config.yml, /settings.json
   - /__ENV.js, /env.js, /config.js (served as static files)
   - Match on patterns: DATABASE_URL, SECRET_KEY, API_KEY, PRIVATE_KEY, AWS_

2. `api-auth-bypass.yaml` — Check for API endpoints accessible without authentication:
   - /api/users, /api/admin, /api/config
   - /graphql (introspection query)
   - /api/v1/debug, /api/internal
   - Match on 200 responses with JSON body containing user/admin/config data

3. `debug-endpoints.yaml` — Check for debug/profiling endpoints left enabled:
   - /__debug, /_debug, /debug
   - /_profiler, /profiler
   - /phpinfo.php, /server-info, /server-status
   - /_next/data (Next.js data routes with sensitive info)
   - /api/__nextauth (NextAuth debug)
   - Match on 200 with debug/profiling content

4. `database-exposure.yaml` — Check for exposed database admin interfaces:
   - /phpmyadmin, /adminer, /pgadmin
   - /supabase (Studio exposed publicly)
   - /_prisma (Prisma Studio)
   - /drizzle-studio
   - Match on login pages or admin interfaces

5. `admin-panel-detection.yaml` — Check for exposed admin panels:
   - /admin, /dashboard, /wp-admin, /admin.html
   - /strapi/admin, /keystonejs
   - /sanity/desk
   - Match on 200 with admin/login form content

Each template should follow Nuclei YAML format with id, info (name, author: trustedge, severity, description, tags: paid), http requests with matchers. Set severity appropriately:
- env leaks: high
- auth bypass: critical
- debug endpoints: medium-high
- database exposure: critical
- admin panels: medium

Mark all with `tags: paid,trustedge,vibe-code` so they can be filtered by tag.
  </action>
  <verify>
Verify each template file exists and has valid YAML structure with id, info, http, and matchers sections. Check that templates/nuclei/paid/ directory contains exactly 5 files.
  </verify>
  <done>5 paid-tier Nuclei templates exist in templates/nuclei/paid/ covering env leaks, auth bypass, debug endpoints, database exposure, and admin panel detection.</done>
</task>

<task type="auto">
  <name>Task 2: Make orchestrator and scanners tier-aware</name>
  <files>src/orchestrator/worker_pool.rs, src/scanners/vibecode.rs, src/scanners/exposed_files.rs, src/scanners/js_secrets.rs</files>
  <action>
1. Update `src/scanners/js_secrets.rs`:
   - Find the constant or hardcoded limit for max JS files to scan (currently ~20 or similar)
   - Add a `max_files` parameter to `scan_js_secrets` function signature: `pub async fn scan_js_secrets(target_url: &str, max_files: usize) -> Result<Vec<Finding>, ScannerError>`
   - Default to 20 for free tier, 50 for paid tier (caller decides)
   - If changing the function signature breaks existing callers, that's fine — the orchestrator is the only caller and we're updating it

2. Update `src/scanners/exposed_files.rs`:
   - Add an `extended` boolean parameter: `pub async fn scan_exposed_files(target_url: &str, extended: bool) -> Result<Vec<Finding>, ScannerError>`
   - When `extended` is true, add additional probe paths: /backup, /dump.sql, /database.sql, /db.sql, /.git/HEAD, /.svn/entries, /.DS_Store, /wp-config.php, /composer.json, /package-lock.json, /yarn.lock, /Gemfile.lock, /.htaccess, /.htpasswd, /crossdomain.xml, /robots.txt (parsed for disallowed paths), /sitemap.xml
   - When false, use existing path list only (free tier behavior unchanged)

3. Update `src/scanners/vibecode.rs`:
   - Add a `tier` parameter: `pub async fn scan_vibecode(target_url: &str, framework: Option<&str>, platform: Option<&str>, tier: &str) -> Result<Vec<Finding>, ScannerError>`
   - When tier is "paid", include templates from BOTH `templates/nuclei/` (free) AND `templates/nuclei/paid/` directories
   - When tier is "free", include only `templates/nuclei/` (existing behavior)
   - Update the Docker run command or template path to include the paid directory

4. Update `src/orchestrator/worker_pool.rs`:
   - Add a `spawn_paid_scan` method to ScanOrchestrator that accepts scan_id and target_url, same as spawn_scan but with tier="paid"
   - Refactor `execute_scan_internal` to accept a `tier: &str` parameter
   - Based on tier:
     - Free: timeout 180s for vibecode (existing), 60s for others; max_files=20; extended=false; tier="free" for vibecode
     - Paid: timeout 600s overall; max_files=50; extended=true; tier="paid" for vibecode
   - Update `run_scanners` to accept tier parameter and pass appropriate values to each scanner
   - Keep `spawn_scan` (free tier) behavior unchanged — it calls execute_scan_internal with tier="free"
   - `spawn_paid_scan` calls execute_scan_internal with tier="paid"
   - For paid scans, set a longer overall timeout (600s) and pass extended parameters to each scanner

IMPORTANT: Do NOT break free tier functionality. The existing `spawn_scan` must continue working exactly as before. Add tier awareness as an extension, not a replacement. The simplest approach: add `tier` parameter to execute_scan_internal and run_scanners, default to "free" in existing spawn_scan path.
  </action>
  <verify>
`cargo check` passes. Verify:
- js_secrets::scan_js_secrets now accepts max_files parameter
- exposed_files::scan_exposed_files now accepts extended parameter
- vibecode::scan_vibecode now accepts tier parameter
- ScanOrchestrator has spawn_paid_scan method
- Existing spawn_scan method still works (calls with free tier defaults)
  </verify>
  <done>Orchestrator is tier-aware. Paid scans run with extended Nuclei templates (free + paid directories), 50 JS files (vs 20), extended exposed file paths, and 600s timeout. Free tier behavior is unchanged. 5 new paid-only Nuclei templates exist.</done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `templates/nuclei/paid/` contains 5 YAML template files
- `src/orchestrator/worker_pool.rs` has both `spawn_scan` (free) and `spawn_paid_scan` (paid)
- Scanner functions accept tier/extended/max_files parameters
- Free tier scan behavior is identical to pre-Phase 4
</verification>

<success_criteria>
- `cargo check` compiles successfully
- Paid tier consistently scans deeper (more templates, more files, more paths)
- Free tier is unchanged (no regressions)
- Orchestrator can be called from webhook handler to trigger paid scan
</success_criteria>

<output>
After completion, create `.planning/phases/04-monetization/04-03-SUMMARY.md`
</output>
