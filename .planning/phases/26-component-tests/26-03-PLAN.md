---
phase: 26-component-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/__tests__/components/ProgressChecklist.test.tsx
  - frontend/__tests__/components/UpgradeCTA.test.tsx
  - frontend/__tests__/components/Footer.test.tsx
  - frontend/__tests__/components/Logo.test.tsx
autonomous: true
requirements:
  - COMP-05
  - COMP-06
  - COMP-07
  - COMP-08
  - COMP-09
must_haves:
  truths:
    - "ProgressChecklist renders all 6 scan stages with correct labels"
    - "ProgressChecklist shows checkmarks for completed stages and pending indicators for incomplete stages"
    - "ProgressChecklist shows description text for the currently active stage"
    - "UpgradeCTA renders pricing information and upgrade button"
    - "UpgradeCTA shows loading state during checkout and error state on failure"
    - "Footer renders Privacy Policy and Terms of Service links"
    - "Footer renders OSS attribution for Nuclei and testssl.sh"
    - "Logo renders image with ShipSecure alt text for all size variants"
    - "Header tests from Phase 25 remain passing (COMP-07 satisfied)"
  artifacts:
    - path: "frontend/__tests__/components/ProgressChecklist.test.tsx"
      provides: "ProgressChecklist component tests"
      min_lines: 50
    - path: "frontend/__tests__/components/UpgradeCTA.test.tsx"
      provides: "UpgradeCTA component tests"
      min_lines: 60
    - path: "frontend/__tests__/components/Footer.test.tsx"
      provides: "Footer component tests"
      min_lines: 40
    - path: "frontend/__tests__/components/Logo.test.tsx"
      provides: "Logo component tests"
      min_lines: 30
  key_links:
    - from: "frontend/__tests__/components/UpgradeCTA.test.tsx"
      to: "frontend/components/upgrade-cta.tsx"
      via: "import, render, and MSW mock for checkout endpoint"
      pattern: "import.*UpgradeCTA.*upgrade-cta"
---

<objective>
Write tests for ProgressChecklist, UpgradeCTA, Footer, and Logo components. Also confirm COMP-07 (Header) is satisfied by existing Phase 25 tests.

Purpose: ProgressChecklist and UpgradeCTA are in the scan and payment flow. Footer and Logo are layout components needing basic rendering verification. Together these cover the remaining non-results components.
Output: Four test files with 20-25 tests total.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-component-tests/26-RESEARCH.md
@frontend/components/progress-checklist.tsx
@frontend/components/upgrade-cta.tsx
@frontend/components/footer.tsx
@frontend/components/logo.tsx
@frontend/__tests__/components/Header.test.tsx
@frontend/__tests__/helpers/test-utils.tsx
@frontend/__tests__/helpers/msw/server.ts
@frontend/__tests__/helpers/msw/handlers.ts
@frontend/vitest.setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProgressChecklist and UpgradeCTA test files</name>
  <files>frontend/__tests__/components/ProgressChecklist.test.tsx, frontend/__tests__/components/UpgradeCTA.test.tsx</files>
  <action>
**ProgressChecklist.test.tsx** — 7-8 tests:

The component accepts `stages` (6 booleans) and `status` (string) props.

Test groups:
1. **Stage Labels (1 test):**
   - Renders all 6 stage labels — pass all stages as `false`, status `'in_progress'`, assert "Detecting Framework", "Security Headers", "TLS Configuration", "Exposed Files", "JavaScript Secrets", "Vibe-Code Scan" all visible via `screen.getByText()`

2. **Completed Stages (2 tests):**
   - Shows checkmark for completed stages — pass `stages: { detection: true, headers: true, tls: false, files: false, secrets: false, vibecode: false }`, status `'in_progress'`, assert checkmark characters are present for completed items
   - Shows pending indicator for incomplete stages — same fixture, assert pending indicators for non-completed stages

3. **Active Stage Description (2 tests):**
   - Shows description for active stage (first incomplete) — with detection+headers done, assert description for "TLS Configuration" stage is visible ("Analyzing certificate validity...")
   - Does NOT show descriptions for non-active incomplete stages — assert "Probing for .env" (files description) is NOT visible

4. **State Transitions via Rerender (2 tests):**
   - Re-render with more stages complete updates display — initial render with 2 stages done, `rerender()` with 4 stages done, assert newly completed stages show checkmarks
   - Failed status shows failure indicators — pass `status: 'failed'`, assert failure markers visible (the "✗" character)

**UpgradeCTA.test.tsx** — 8-10 tests:

The component accepts `scanId` and `token` props and uses `fetch` for checkout.

Test groups:
1. **Rendering (3 tests):**
   - Renders "Upgrade to Deep Audit" heading — `screen.getByRole('heading', { name: /upgrade to deep audit/i })`
   - Renders upgrade button with "$49" price — `screen.getByRole('button', { name: /upgrade for \$49/i })`
   - Renders feature list items — assert "10x more checks", "Active probing", "PDF report", "Extended JS analysis" text visible

2. **Checkout Flow (3 tests):**
   - Button shows loading text during checkout — use `userEvent.setup()`, click the upgrade button, assert button text changes to "Redirecting to checkout..." via `findByText` (async)
   - Calls checkout API on click — use MSW `server.use()` to override POST handler for `http://localhost:3000/api/v1/checkout` returning `{ checkout_url: 'https://checkout.stripe.com/test' }`. Click button. Use `waitFor` or `findByText` to verify loading state appeared.
   - Shows error message when checkout fails — use `server.use()` with handler returning 500 `{ title: 'Payment service unavailable' }`. Click button. Assert error text "Payment service unavailable" appears via `findByText`.

3. **Error Display (2 tests):**
   - Error message is displayed in error banner — after triggering error (from checkout flow test), assert the error text is within a visible element
   - Button re-enables after error — after error, assert button is NOT disabled (loading state cleared)

**MSW handler for UpgradeCTA checkout tests:**
```typescript
import { server } from '@/__tests__/helpers/msw/server'
import { http, HttpResponse } from 'msw'

// In test: override the default handler
server.use(
  http.post('http://localhost:3000/api/v1/checkout', () => {
    return HttpResponse.json({ checkout_url: 'https://checkout.stripe.com/test-session' })
  })
)
```

**Important:** The component calls `window.location.href = data.checkout_url` on success. In tests, mock `window.location` to prevent navigation errors:
```typescript
beforeEach(() => {
  Object.defineProperty(window, 'location', {
    value: { href: '' },
    writable: true,
    configurable: true,
  })
})
```
  </action>
  <verify>Run `cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run __tests__/components/ProgressChecklist.test.tsx __tests__/components/UpgradeCTA.test.tsx` — all tests pass.</verify>
  <done>ProgressChecklist.test.tsx has 7-8 passing tests covering stage labels, completion states, active stage description, rerender transitions, and failed status. UpgradeCTA.test.tsx has 8-10 passing tests covering rendering, checkout flow with MSW, and error display.</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer and Logo test files, verify Header tests pass</name>
  <files>frontend/__tests__/components/Footer.test.tsx, frontend/__tests__/components/Logo.test.tsx</files>
  <action>
**Footer.test.tsx** — 5-6 tests:

The Footer component uses next/link (already mocked globally). It renders legal links, copyright, and OSS attribution.

Test groups:
1. **Legal Links (2 tests):**
   - Renders Privacy Policy link — `screen.getByRole('link', { name: /privacy policy/i })`, assert `toHaveAttribute('href', '/privacy')`
   - Renders Terms of Service link — `screen.getByRole('link', { name: /terms of service/i })`, assert `toHaveAttribute('href', '/terms')`

2. **Copyright (1 test):**
   - Renders copyright with current year — assert `screen.getByText()` matching copyright pattern with current year (e.g., `new RegExp(String(new Date().getFullYear()))`)

3. **OSS Attribution (2-3 tests):**
   - Renders "Powered by open source" text — `screen.getByText(/powered by open source/i)`
   - Renders Nuclei attribution link — `screen.getByRole('link', { name: /nuclei/i })`, assert href contains "projectdiscovery/nuclei"
   - Renders testssl.sh attribution link — `screen.getByRole('link', { name: /testssl\.sh/i })`, assert href contains "testssl.sh"

**Logo.test.tsx** — 5-6 tests:

The Logo component renders a next/image (mocked to plain img tag via vitest.setup.ts). It accepts `size` prop ('small' | 'medium' | 'large').

Test groups:
1. **Size Variants (3 tests):**
   - Renders small variant — `renderWithProviders(<Logo size="small" />)`, assert img element exists with `alt="ShipSecure"` and `width="96"` and `height="64"`
   - Renders medium variant — assert width="384" height="256"
   - Renders large variant — assert width="768" height="512"

2. **Common Attributes (2 tests):**
   - All variants use correct src — assert `src="/logo.png"` for any size
   - Passes className prop — render with `className="test-class"`, assert img has "test-class" class via `toHaveClass` or `getAttribute('class')`

**COMP-07 Header verification:**
Do NOT create new Header tests. Simply verify existing Phase 25 Header tests still pass:
```bash
npx vitest run __tests__/components/Header.test.tsx
```
If they pass, COMP-07 is satisfied. Note this in the SUMMARY.
  </action>
  <verify>Run `cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run __tests__/components/Footer.test.tsx __tests__/components/Logo.test.tsx __tests__/components/Header.test.tsx` — all tests pass.</verify>
  <done>Footer.test.tsx has 5-6 passing tests covering legal links, copyright, and OSS attribution. Logo.test.tsx has 5-6 passing tests covering all three size variants and common attributes. Existing Header.test.tsx from Phase 25 still passes (COMP-07 satisfied).</done>
</task>

</tasks>

<verification>
```bash
cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run __tests__/components/ProgressChecklist.test.tsx __tests__/components/UpgradeCTA.test.tsx __tests__/components/Footer.test.tsx __tests__/components/Logo.test.tsx __tests__/components/Header.test.tsx
```
All tests pass. Combined test count is 25-30 (including Header's existing 4).
</verification>

<success_criteria>
- ProgressChecklist tests cover all 6 stage labels, completion checkmarks, active stage description, rerender transitions, and failed status
- UpgradeCTA tests cover pricing display, checkout API call with MSW, loading state, error display, and button re-enable after error
- Footer tests cover legal links (Privacy Policy, Terms of Service), copyright year, and OSS attribution (Nuclei, testssl.sh)
- Logo tests cover all three size variants (small, medium, large) with correct dimensions
- Header existing tests from Phase 25 still pass (COMP-07 satisfied without new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/26-component-tests/26-03-SUMMARY.md`
</output>
