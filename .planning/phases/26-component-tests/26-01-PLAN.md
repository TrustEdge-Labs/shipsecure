---
phase: 26-component-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/__tests__/components/ScanForm.test.tsx
autonomous: true
requirements:
  - COMP-01
must_haves:
  truths:
    - "ScanForm renders URL input, email input, CFAA consent checkbox, and submit button"
    - "ScanForm displays field-level validation errors for URL, email, and authorization"
    - "ScanForm shows loading state with disabled button and 'Starting scan...' text when pending"
    - "ScanForm shows success message with redirect text when scanId is present"
    - "ScanForm allows user to type in fields and toggle consent checkbox"
  artifacts:
    - path: "frontend/__tests__/components/ScanForm.test.tsx"
      provides: "Comprehensive ScanForm component tests"
      min_lines: 100
  key_links:
    - from: "frontend/__tests__/components/ScanForm.test.tsx"
      to: "frontend/components/scan-form.tsx"
      via: "import and render"
      pattern: "import.*ScanForm.*scan-form"
---

<objective>
Write comprehensive tests for the ScanForm component covering form fields, validation errors, loading state, success state, and user interactions.

Purpose: ScanForm is the primary revenue entry point — the first component users interact with. Thorough testing catches regressions in the scan submission flow.
Output: `frontend/__tests__/components/ScanForm.test.tsx` with 12-15 tests.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-component-tests/26-RESEARCH.md
@frontend/components/scan-form.tsx
@frontend/app/actions/scan.ts
@frontend/__tests__/helpers/test-utils.tsx
@frontend/vitest.setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScanForm test file with useActionState mock and comprehensive tests</name>
  <files>frontend/__tests__/components/ScanForm.test.tsx</files>
  <action>
Create `frontend/__tests__/components/ScanForm.test.tsx` with the following structure:

**Setup — useActionState mock (CRITICAL PATTERN):**
```typescript
let mockState = {} as any
let mockFormAction = vi.fn()
let mockPending = false

vi.mock('react', async () => {
  const actual = await vi.importActual('react')
  return {
    ...actual,
    useActionState: vi.fn(() => [mockState, mockFormAction, mockPending])
  }
})
```
- MUST spread `...actual` to preserve useState, useEffect, etc.
- Reset `mockState`, `mockFormAction`, `mockPending` in `beforeEach`
- Import from vitest: `vi, describe, test, expect, beforeEach`
- Import `screen` from `@testing-library/react`
- Import `userEvent` from `@testing-library/user-event`
- Import `renderWithProviders` from `@/__tests__/helpers/test-utils`
- Import `ScanForm` from `@/components/scan-form`

**Test groups — use describe blocks:**

1. **Form Fields (4 tests):**
   - Renders URL input with label "Website URL" — use `screen.getByLabelText(/website url/i)`
   - Renders email input with label containing "Email" — use `screen.getByLabelText(/email/i)`
   - Renders CFAA consent checkbox — use `screen.getByRole('checkbox')` and verify label text contains "I confirm I own this website"
   - Renders submit button with "Scan Now" text — use `screen.getByRole('button', { name: /scan now/i })`

2. **Validation Errors (4 tests):**
   - Displays URL validation error when `mockState = { errors: { url: ['Please enter a valid URL'] } }` — assert `screen.getByText(/please enter a valid url/i)`
   - Displays email validation error when `mockState = { errors: { email: ['Please enter a valid email address'] } }` — assert text visible
   - Displays authorization error when `mockState = { errors: { authorization: ['You must confirm you have authorization'] } }` — assert text visible
   - Displays form-level error when `mockState = { errors: { _form: ['Unable to connect to the scanning service'] } }` — assert text visible in error banner

3. **Loading State (2 tests):**
   - Button text changes to "Starting scan..." when `mockPending = true` — use `screen.getByRole('button', { name: /starting scan/i })`
   - Button is disabled when `mockPending = true` — assert `toBeDisabled()`

4. **Success State (1 test):**
   - Renders success message when `mockState = { scanId: 'scan-123' }` — assert "Scan started!" heading and "Redirecting to your scan progress" text visible

5. **User Interactions (3 tests):**
   - User can type in URL field — `const user = userEvent.setup()`, `await user.type(urlInput, 'https://example.com')`, assert `toHaveValue('https://example.com')`
   - User can type in email field — same pattern with email
   - User can toggle consent checkbox — `await user.click(checkbox)`, assert `toBeChecked()`, click again, assert `not.toBeChecked()`

**Key rules:**
- Call `userEvent.setup()` at the START of each interaction test (not in beforeEach)
- Use accessible queries: getByLabelText for form fields, getByRole for buttons/checkboxes, getByText for content
- All user-event calls must be awaited
- Each test sets mock state BEFORE rendering (not after)
  </action>
  <verify>Run `cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run __tests__/components/ScanForm.test.tsx` — all tests pass.</verify>
  <done>ScanForm.test.tsx exists with 12-15 passing tests covering form fields, validation errors, loading state, success state, and user interactions.</done>
</task>

</tasks>

<verification>
```bash
cd /home/john/vault/projects/github.com/shipsecure/frontend && npx vitest run __tests__/components/ScanForm.test.tsx
```
All tests pass. Test count is 12-15.
</verification>

<success_criteria>
- ScanForm.test.tsx has 12+ passing tests
- Tests cover: form fields, URL/email/authorization/form-level validation errors, loading state, success state, user interactions (typing, checkbox toggle)
- useActionState mock correctly preserves other React hooks
- All queries use accessible patterns (getByLabelText, getByRole, getByText)
</success_criteria>

<output>
After completion, create `.planning/phases/26-component-tests/26-01-SUMMARY.md`
</output>
